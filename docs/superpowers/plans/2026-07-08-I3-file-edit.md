# I3 — 文件预览/编辑 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把只读的 M7 文件面板升级为可编辑文本（CodeMirror 多语言）+ 图片/PDF 富预览 + 新建/删除 + mtime 冲突提醒的文件面板。

**Architecture:** 后端在 root 锁定 cwd 的 `FileService` 上加 `write`/`remove`/`statFile`，HTTP 加 `PUT`/`DELETE /api/files/content` 与 `GET /api/files/raw`（流式、按扩展名给 MIME）。前端把视图按扩展名分流（text→CodeMirror 编辑器 / image→`<img>` / pdf→`<iframe>` / other→"无法展示+下载"）；CodeMirror 包在独立 `CodeEditor` 组件里（jsdom 测不了它，故 FilesPanel 单测 mock 掉它）。所有网络调用注入，面板可脱网单测。

**Tech Stack:** Node `node:fs`/流；`@uiw/react-codemirror` + `@codemirror/lang-*` + `@codemirror/legacy-modes`；Vitest + @testing-library/react（jsdom）。

**测试命令**：后端 `pnpm -F @zouyj/zuse-server exec vitest run <file>`；前端 `pnpm -F @zuse/web exec vitest run <file>`；类型检查 `pnpm -F @zouyj/zuse-server exec tsc --noEmit` / `pnpm -F @zuse/web typecheck`。
> 注：server 包名是 `@zouyj/zuse-server`（不是 @zuse/server）。protocol 改动后需先 `pnpm -F @zuse/protocol build` 或让 workspace 解析到源码（本仓库 protocol 是 type-only，import type 直接读源码 `.ts`，一般无需 build；若 server/web typecheck 找不到新类型，跑一次 `pnpm -r typecheck` 前确保 protocol 源码已存新类型即可）。

---

## 文件结构

**后端**
- 改 `packages/protocol/src/index.ts` — `FilePreview.mtimeMs`；新增 `WriteFileResult`、`WriteFileBody`。
- 改 `packages/server/src/file/FileService.ts` — `read` 加 mtimeMs；新增 `write`/`remove`/`statFile`/`FileChangedError`/`mimeForExt`。
- 改 `packages/server/src/http/server.ts` — `PUT`/`DELETE /api/files/content`、`GET /api/files/raw`。
- 改 `packages/server/src/file/FileService.test.ts` — write/remove/statFile 测试。
- 改 `packages/server/src/http/server.test.ts`（若存在则加用例；不存在则新建最小 http 测试文件）——本计划在有测试文件时追加；HTTP 层主要靠 FileService 单测 + 前端 + 浏览器覆盖。

**前端**
- 新增 `packages/web/src/components/fileLang.ts` (+ `.test.ts`) — 扩展名→CodeMirror 语言 extensions。
- 新增 `packages/web/src/components/fileView.ts` (+ `.test.ts`) — 扩展名→视图模式 `classify()`。
- 新增 `packages/web/src/components/CodeEditor.tsx` — CodeMirror 薄封装（value/onChange/path/onSave）。
- 改 `packages/web/src/state/manageApi.ts` — `writeFile`/`deleteFile`/`rawFileUrl` + `FileConflictError`。
- 改 `packages/web/src/components/FilesPanel.tsx` (+ `.test.tsx`) — 视图分流、编辑、保存、冲突、新建、删除、未保存守卫。
- 改 `packages/web/src/components/ManageDrawer.tsx` — 接线新注入函数。
- 改 `packages/web/src/styles.css` — 编辑器/预览/新建/删除样式。
- 改 `packages/web/package.json` — CodeMirror 依赖。

---

## Task 1: protocol 类型

**Files:** Modify `packages/protocol/src/index.ts`

- [ ] **Step 1: 加字段与类型**

在 `FilePreview` 接口里，`size` 字段旁加：
```ts
  /** Last-modified time (ms since epoch) — used for save-time conflict detection. */
  mtimeMs: number
```
在 `FilePreview` 定义之后加：
```ts
/** Result of writing a file (edit or create). */
export interface WriteFileResult {
  path: string
  size: number
  mtimeMs: number
}

/** Body for PUT /api/files/content. expectMtimeMs absent = create / no conflict check; force skips it. */
export interface WriteFileBody {
  path: string
  content: string
  expectMtimeMs?: number
  force?: boolean
}
```

- [ ] **Step 2: 类型检查**

Run: `pnpm -F @zuse/protocol exec tsc --noEmit` （若无该脚本则 `pnpm -r typecheck` 只看 protocol 段）
Expected: 通过。（此步会让 `FileService.read` 现有返回缺 `mtimeMs` 而报错——Task 2 修，可先继续。）

- [ ] **Step 3: 提交**

```bash
git add packages/protocol/src/index.ts
git commit -m "feat(protocol): I3 FilePreview.mtimeMs + WriteFileResult/WriteFileBody"
```

---

## Task 2: FileService.read 加 mtimeMs + write()

**Files:** Modify `packages/server/src/file/FileService.ts`, Test `packages/server/src/file/FileService.test.ts`

- [ ] **Step 1: 写失败测试**（追加到 `FileService.test.ts`）

```ts
import { FileService, PathOutsideRootError, FileChangedError } from './FileService.js'
import { mkdtemp, writeFile, readFile, stat, mkdir } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { join } from 'node:path'

async function tmpRoot(): Promise<string> { return mkdtemp(join(tmpdir(), 'i3-')) }

describe('FileService.write', () => {
  it('writes new content and returns size + mtimeMs', async () => {
    const root = await tmpRoot()
    const svc = new FileService(root)
    const r = await svc.write('note.txt', 'hello')
    expect(await readFile(join(root, 'note.txt'), 'utf8')).toBe('hello')
    expect(r.path).toBe('note.txt')
    expect(r.size).toBe(5)
    expect(typeof r.mtimeMs).toBe('number')
  })

  it('creates a file at a non-existent path (parent exists)', async () => {
    const root = await tmpRoot()
    await mkdir(join(root, 'sub'))
    const svc = new FileService(root)
    await svc.write('sub/new.md', '# x')
    expect(await readFile(join(root, 'sub/new.md'), 'utf8')).toBe('# x')
  })

  it('rejects a path escaping the root', async () => {
    const svc = new FileService(await tmpRoot())
    await expect(svc.write('../evil.txt', 'x')).rejects.toBeInstanceOf(PathOutsideRootError)
  })

  it('throws FileChangedError when expectMtimeMs does not match', async () => {
    const root = await tmpRoot()
    const svc = new FileService(root)
    await svc.write('a.txt', 'one')
    await expect(svc.write('a.txt', 'two', { expectMtimeMs: 1 })).rejects.toBeInstanceOf(FileChangedError)
    expect(await readFile(join(root, 'a.txt'), 'utf8')).toBe('one') // unchanged
  })

  it('overwrites despite mismatch when force is set', async () => {
    const root = await tmpRoot()
    const svc = new FileService(root)
    await svc.write('a.txt', 'one')
    await svc.write('a.txt', 'two', { expectMtimeMs: 1, force: true })
    expect(await readFile(join(root, 'a.txt'), 'utf8')).toBe('two')
  })

  it('write with matching expectMtimeMs succeeds', async () => {
    const root = await tmpRoot()
    const svc = new FileService(root)
    await svc.write('a.txt', 'one')
    const cur = (await stat(join(root, 'a.txt'))).mtimeMs
    await svc.write('a.txt', 'two', { expectMtimeMs: cur })
    expect(await readFile(join(root, 'a.txt'), 'utf8')).toBe('two')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zouyj/zuse-server exec vitest run src/file/FileService.test.ts`
Expected: FAIL（`write` / `FileChangedError` 未定义）

- [ ] **Step 3: 写实现**

`FileService.ts` 顶部 import 补 `writeFile`、`unlink`、`extname`：
```ts
import { readdir, stat, open, writeFile, unlink } from 'node:fs/promises'
import { resolve, relative, join, sep, isAbsolute, extname } from 'node:path'
import type { DirListing, FileEntry, FilePreview, WriteFileResult } from '@zuse/protocol'
```
在 `PathOutsideRootError` 类之后加：
```ts
export class FileChangedError extends Error {
  constructor() {
    super('file changed on disk since it was loaded')
    this.name = 'FileChangedError'
  }
}
```
把 `read()` 结尾两个 `return` 各加 `mtimeMs: st.mtimeMs`：
```ts
      if (head.includes(0)) return { path, content: '', truncated: false, binary: true, size: st.size, mtimeMs: st.mtimeMs }
      // ...
      return { path, content: buf.toString('utf8'), truncated: st.size > PREVIEW_CAP, binary: false, size: st.size, mtimeMs: st.mtimeMs }
```
在 `read()` 方法之后加：
```ts
  /** Write a file (edit or create). Optional mtime guard rejects a stale overwrite unless forced. */
  async write(relPath: string, content: string, opts: { expectMtimeMs?: number; force?: boolean } = {}): Promise<WriteFileResult> {
    const abs = this.resolveInRoot(relPath)
    // Refuse to clobber a directory. stat may throw ENOENT for a new file — that's fine (create).
    let existing: Awaited<ReturnType<typeof stat>> | null = null
    try { existing = await stat(abs) } catch { existing = null }
    if (existing?.isDirectory()) throw new Error('path is a directory')
    if (opts.expectMtimeMs != null && !opts.force && existing && existing.mtimeMs !== opts.expectMtimeMs) {
      throw new FileChangedError()
    }
    await writeFile(abs, content, 'utf8')
    const st = await stat(abs)
    return { path: this.toRel(abs), size: st.size, mtimeMs: st.mtimeMs }
  }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zouyj/zuse-server exec vitest run src/file/FileService.test.ts`
Expected: PASS（write 6 条 + 既有 read/list 测试）

- [ ] **Step 5: 提交**

```bash
git add packages/server/src/file/FileService.ts packages/server/src/file/FileService.test.ts
git commit -m "feat(server): I3 FileService.write (edit/create) + mtime conflict + read mtimeMs"
```

---

## Task 3: FileService.remove()

**Files:** Modify `packages/server/src/file/FileService.ts`, Test `FileService.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
describe('FileService.remove', () => {
  it('deletes a file', async () => {
    const root = await tmpRoot()
    const svc = new FileService(root)
    await svc.write('gone.txt', 'x')
    await svc.remove('gone.txt')
    await expect(stat(join(root, 'gone.txt'))).rejects.toMatchObject({ code: 'ENOENT' })
  })

  it('refuses to delete a directory', async () => {
    const root = await tmpRoot()
    await mkdir(join(root, 'dir'))
    const svc = new FileService(root)
    await expect(svc.remove('dir')).rejects.toThrow('directory')
  })

  it('rejects a path escaping the root', async () => {
    const svc = new FileService(await tmpRoot())
    await expect(svc.remove('../x')).rejects.toBeInstanceOf(PathOutsideRootError)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zouyj/zuse-server exec vitest run src/file/FileService.test.ts`
Expected: FAIL（`remove` 未定义）

- [ ] **Step 3: 写实现** — 在 `write()` 之后加：

```ts
  /** Delete a file (not a directory). */
  async remove(relPath: string): Promise<void> {
    const abs = this.resolveInRoot(relPath)
    const st = await stat(abs)
    if (st.isDirectory()) throw new Error('path is a directory')
    await unlink(abs)
  }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zouyj/zuse-server exec vitest run src/file/FileService.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/server/src/file/FileService.ts packages/server/src/file/FileService.test.ts
git commit -m "feat(server): I3 FileService.remove (files only)"
```

---

## Task 4: FileService.statFile() + MIME 映射

**Files:** Modify `packages/server/src/file/FileService.ts`, Test `FileService.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
describe('FileService.statFile', () => {
  it('returns mime by extension', async () => {
    const root = await tmpRoot()
    const svc = new FileService(root)
    await svc.write('pic.png', 'x'); await svc.write('doc.pdf', 'x'); await svc.write('blob.bin', 'x')
    expect((await svc.statFile('pic.png')).mime).toBe('image/png')
    expect((await svc.statFile('doc.pdf')).mime).toBe('application/pdf')
    expect((await svc.statFile('blob.bin')).mime).toBe('application/octet-stream')
  })
  it('rejects a directory and an escaping path', async () => {
    const root = await tmpRoot()
    await mkdir(join(root, 'd'))
    const svc = new FileService(root)
    await expect(svc.statFile('d')).rejects.toThrow('directory')
    await expect(svc.statFile('../x')).rejects.toBeInstanceOf(PathOutsideRootError)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zouyj/zuse-server exec vitest run src/file/FileService.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现** — 在文件顶部（class 外）加 MIME 表，class 内加 `statFile`：

class 外、`PREVIEW_CAP` 附近：
```ts
/** Extension → MIME for the raw byte endpoint (image/pdf inline preview + download). */
const MIME_BY_EXT: Record<string, string> = {
  '.png': 'image/png', '.jpg': 'image/jpeg', '.jpeg': 'image/jpeg', '.gif': 'image/gif',
  '.webp': 'image/webp', '.bmp': 'image/bmp', '.avif': 'image/avif', '.ico': 'image/x-icon',
  '.svg': 'image/svg+xml', '.pdf': 'application/pdf',
}
export function mimeForExt(name: string): string {
  return MIME_BY_EXT[extname(name).toLowerCase()] ?? 'application/octet-stream'
}
```
class 内、`remove()` 之后：
```ts
  /** Absolute path + size + MIME for the raw byte endpoint. Rejects directories / escaping paths. */
  async statFile(relPath: string): Promise<{ abs: string; size: number; mime: string }> {
    const abs = this.resolveInRoot(relPath)
    const st = await stat(abs)
    if (st.isDirectory()) throw new Error('path is a directory')
    return { abs, size: st.size, mime: mimeForExt(abs) }
  }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zouyj/zuse-server exec vitest run src/file/FileService.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/server/src/file/FileService.ts packages/server/src/file/FileService.test.ts
git commit -m "feat(server): I3 FileService.statFile + extension→MIME map"
```

---

## Task 5: HTTP — PUT / DELETE /api/files/content

**Files:** Modify `packages/server/src/http/server.ts`

> HTTP 层无独立单测框架时靠 FileService 单测 + 前端 + 浏览器覆盖；本任务加路由并 typecheck。若 `server.test.ts` 存在且有 http 拨测夹具，按其风格补 PUT/DELETE 用例（可选）。

- [ ] **Step 1: 加路由** — 在现有 `if (method === 'GET' && (path === '/api/files' || path === '/api/files/content'))` 块**之后**加：

```ts
    // PUT /api/files/content — write (edit or create) a file. DELETE — remove a file. (I3)
    if ((method === 'PUT' || method === 'DELETE') && path === '/api/files/content') {
      if (!isAuthed(req)) return sendJson(res, 401, { error: { code: 'unauthorized', message: 'Not authenticated' } })
      try {
        const cwd = url.searchParams.get('cwd')
        const fileSvc = cwd ? new FileService(cwd) : deps.file
        if (method === 'DELETE') {
          const p = url.searchParams.get('path')
          if (!p) return sendJson(res, 400, { error: { code: 'bad_request', message: 'Missing path' } })
          await fileSvc.remove(p)
          return sendJson(res, 200, { ok: true })
        }
        const body = (await readJsonBody(req)) as { path?: string; content?: string; expectMtimeMs?: number; force?: boolean }
        if (!body?.path || typeof body.content !== 'string') return sendJson(res, 400, { error: { code: 'bad_request', message: 'path and content required' } })
        const result = await fileSvc.write(body.path, body.content, { expectMtimeMs: body.expectMtimeMs, force: body.force })
        return sendJson(res, 200, result)
      } catch (e) {
        if (e instanceof PathOutsideRootError) return sendJson(res, 403, { error: { code: 'forbidden', message: e.message } })
        if (e instanceof FileChangedError) return sendJson(res, 409, { error: { code: 'conflict', message: e.message } })
        if ((e as NodeJS.ErrnoException).code === 'ENOENT') return sendJson(res, 404, { error: { code: 'not_found', message: 'Not found' } })
        return sendJson(res, 400, { error: { code: 'bad_request', message: (e as Error).message } })
      }
    }
```
文件顶部 import 补 `FileChangedError`：
```ts
import { FileService, PathOutsideRootError, FileChangedError } from '../file/FileService.js'
```

- [ ] **Step 2: 类型检查**

Run: `pnpm -F @zouyj/zuse-server exec tsc --noEmit`
Expected: 通过

- [ ] **Step 3: 提交**

```bash
git add packages/server/src/http/server.ts
git commit -m "feat(server): I3 PUT/DELETE /api/files/content (write/create/delete)"
```

---

## Task 6: HTTP — GET /api/files/raw（流式字节）

**Files:** Modify `packages/server/src/http/server.ts`

- [ ] **Step 1: 加路由** — 在 Task 5 的块之后加：

```ts
    // GET /api/files/raw — raw bytes with a real Content-Type, for <img>/<iframe>/download. (I3)
    if (method === 'GET' && path === '/api/files/raw') {
      if (!isAuthed(req)) return sendJson(res, 401, { error: { code: 'unauthorized', message: 'Not authenticated' } })
      try {
        const cwd = url.searchParams.get('cwd')
        const fileSvc = cwd ? new FileService(cwd) : deps.file
        const p = url.searchParams.get('path')
        if (!p) return sendJson(res, 400, { error: { code: 'bad_request', message: 'Missing path' } })
        const info = await fileSvc.statFile(p)
        if (info.size > RAW_CAP) return sendJson(res, 413, { error: { code: 'too_large', message: 'File too large to serve' } })
        res.writeHead(200, { 'content-type': info.mime, 'content-length': String(info.size) })
        createReadStream(info.abs).pipe(res)
        return
      } catch (e) {
        if (e instanceof PathOutsideRootError) return sendJson(res, 403, { error: { code: 'forbidden', message: e.message } })
        if ((e as NodeJS.ErrnoException).code === 'ENOENT') return sendJson(res, 404, { error: { code: 'not_found', message: 'Not found' } })
        return sendJson(res, 400, { error: { code: 'bad_request', message: (e as Error).message } })
      }
    }
```
文件顶部 import 补 `createReadStream` 与常量 `RAW_CAP`：
```ts
import { createReadStream } from 'node:fs'
```
在文件顶部合适的常量区（其它 `const` 附近）加：
```ts
/** Max bytes the raw endpoint will stream inline (50 MiB); larger → 413, client offers download. */
const RAW_CAP = 50 * 1024 * 1024
```

- [ ] **Step 2: 类型检查**

Run: `pnpm -F @zouyj/zuse-server exec tsc --noEmit`
Expected: 通过

- [ ] **Step 3: 手动冒烟（可选，服务器起着时）**

Run: `curl -sI -b <cookie> "http://127.0.0.1:4180/api/files/raw?path=README.md"` → 期望 `Content-Type` 与 `Content-Length`。（无 cookie 会 401；浏览器阶段在 Task 15 统一验。）

- [ ] **Step 4: 提交**

```bash
git add packages/server/src/http/server.ts
git commit -m "feat(server): I3 GET /api/files/raw — mime-typed byte stream (50MiB cap)"
```

---

## Task 7: 前端 fileView.ts — 视图分流

**Files:** Create `packages/web/src/components/fileView.ts` + `fileView.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
import { describe, it, expect } from 'vitest'
import { classify } from './fileView.js'

describe('classify', () => {
  it('images by extension', () => {
    for (const p of ['a.png', 'b.JPG', 'c.jpeg', 'd.gif', 'e.webp', 'f.bmp', 'g.svg', 'h.ico', 'i.avif']) {
      expect(classify(p)).toBe('image')
    }
  })
  it('pdf', () => { expect(classify('doc.PDF')).toBe('pdf') })
  it('text for code/text extensions', () => {
    for (const p of ['a.ts', 'b.js', 'c.md', 'd.json', 'e.txt', 'f.css', 'Dockerfile', 'g.py']) {
      expect(classify(p)).toBe('text')
    }
  })
  it('other for unknown binary-ish', () => {
    for (const p of ['a.xlsx', 'b.docx', 'c.zip', 'd.exe', 'e.mp4']) expect(classify(p)).toBe('other')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/fileView.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 写实现**

```ts
// packages/web/src/components/fileView.ts
export type FileView = 'text' | 'image' | 'pdf' | 'other'

const IMAGE_EXT = new Set(['png', 'jpg', 'jpeg', 'gif', 'webp', 'bmp', 'svg', 'ico', 'avif'])
// Known-binary extensions we can't preview inline → "cannot display + download".
const OTHER_EXT = new Set([
  'xlsx', 'xls', 'docx', 'doc', 'pptx', 'ppt', 'zip', 'gz', 'tar', 'rar', '7z',
  'exe', 'dll', 'so', 'dylib', 'bin', 'wasm', 'mp4', 'mov', 'avi', 'mkv', 'mp3', 'wav', 'flac',
  'woff', 'woff2', 'ttf', 'otf', 'eot',
])

function ext(path: string): string {
  const base = path.split(/[\\/]/).pop() ?? path
  const dot = base.lastIndexOf('.')
  return dot > 0 ? base.slice(dot + 1).toLowerCase() : ''
}

/** Decide how to show a file, by extension. Unknown/no-extension → 'text' (let the content
 * endpoint's binary sniff downgrade it to a "cannot display" state if it turns out binary). */
export function classify(path: string): FileView {
  const e = ext(path)
  if (e === 'pdf') return 'pdf'
  if (IMAGE_EXT.has(e)) return 'image'
  if (OTHER_EXT.has(e)) return 'other'
  return 'text'
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/fileView.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/fileView.ts packages/web/src/components/fileView.test.ts
git commit -m "feat(web): I3 fileView.classify — text/image/pdf/other by extension"
```

---

## Task 8: 安装 CodeMirror 依赖 + fileLang.ts

**Files:** Modify `packages/web/package.json`; Create `packages/web/src/components/fileLang.ts` + `fileLang.test.ts`

- [ ] **Step 1: 安装依赖**

Run:
```bash
pnpm -F @zuse/web add @uiw/react-codemirror @codemirror/lang-javascript @codemirror/lang-json @codemirror/lang-css @codemirror/lang-html @codemirror/lang-markdown @codemirror/lang-python @codemirror/lang-java @codemirror/lang-cpp @codemirror/lang-rust @codemirror/lang-go @codemirror/lang-php @codemirror/lang-sql @codemirror/lang-xml @codemirror/lang-yaml @codemirror/legacy-modes @codemirror/language
```
Expected: 安装成功（联网）。若离线失败 → 停下报告，不要继续（BLOCKED）。

- [ ] **Step 2: 写失败测试**

```ts
import { describe, it, expect } from 'vitest'
import { langExtensions } from './fileLang.js'

describe('langExtensions', () => {
  it('returns a non-empty extension array for known languages', () => {
    for (const p of ['a.ts', 'b.tsx', 'c.js', 'd.json', 'e.py', 'f.css', 'g.html', 'h.md',
                     'i.rs', 'j.go', 'k.java', 'l.cpp', 'm.php', 'n.sql', 'o.xml', 'p.yaml',
                     'run.sh', 'go.bat', 'app.properties', 'Dockerfile', 'x.toml']) {
      expect(langExtensions(p).length).toBeGreaterThan(0)
    }
  })
  it('returns [] for unknown / plain text (still editable)', () => {
    expect(langExtensions('notes.txt')).toEqual([])
    expect(langExtensions('data.unknownext')).toEqual([])
  })
})
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/fileLang.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 4: 写实现**

```ts
// packages/web/src/components/fileLang.ts
import type { Extension } from '@codemirror/state'
import { javascript } from '@codemirror/lang-javascript'
import { json } from '@codemirror/lang-json'
import { css } from '@codemirror/lang-css'
import { html } from '@codemirror/lang-html'
import { markdown } from '@codemirror/lang-markdown'
import { python } from '@codemirror/lang-python'
import { java } from '@codemirror/lang-java'
import { cpp } from '@codemirror/lang-cpp'
import { rust } from '@codemirror/lang-rust'
import { go } from '@codemirror/lang-go'
import { php } from '@codemirror/lang-php'
import { sql } from '@codemirror/lang-sql'
import { xml } from '@codemirror/lang-xml'
import { yaml } from '@codemirror/lang-yaml'
import { StreamLanguage } from '@codemirror/language'
import { shell } from '@codemirror/legacy-modes/mode/shell'
import { properties } from '@codemirror/legacy-modes/mode/properties'
import { toml } from '@codemirror/legacy-modes/mode/toml'
import { dockerFile } from '@codemirror/legacy-modes/mode/dockerfile'
import { ruby } from '@codemirror/legacy-modes/mode/ruby'
import { lua } from '@codemirror/legacy-modes/mode/lua'
import { powerShell } from '@codemirror/legacy-modes/mode/powershell'

const legacy = (m: Parameters<typeof StreamLanguage.of>[0]): Extension => StreamLanguage.of(m)

/** Extension (or filename) → CodeMirror language extensions. Empty = plain text (still editable). */
const BY_EXT: Record<string, () => Extension[]> = {
  js: () => [javascript()], jsx: () => [javascript({ jsx: true })],
  ts: () => [javascript({ typescript: true })], tsx: () => [javascript({ typescript: true, jsx: true })],
  mjs: () => [javascript()], cjs: () => [javascript()],
  json: () => [json()], jsonc: () => [json()],
  css: () => [css()], scss: () => [css()], less: () => [css()],
  html: () => [html()], htm: () => [html()], vue: () => [html()],
  md: () => [markdown()], markdown: () => [markdown()],
  py: () => [python()], java: () => [java()],
  c: () => [cpp()], h: () => [cpp()], cc: () => [cpp()], cpp: () => [cpp()], hpp: () => [cpp()], cxx: () => [cpp()],
  rs: () => [rust()], go: () => [go()],
  php: () => [php()], sql: () => [sql()],
  xml: () => [xml()], svg: () => [xml()],
  yaml: () => [yaml()], yml: () => [yaml()],
  sh: () => [legacy(shell)], bash: () => [legacy(shell)], zsh: () => [legacy(shell)],
  bat: () => [legacy(shell)], cmd: () => [legacy(shell)],
  properties: () => [legacy(properties)], ini: () => [legacy(properties)], env: () => [legacy(properties)], conf: () => [legacy(properties)],
  toml: () => [legacy(toml)],
  rb: () => [legacy(ruby)], lua: () => [legacy(lua)],
  ps1: () => [legacy(powerShell)], psm1: () => [legacy(powerShell)],
}
// Filenames (no extension) with a known language.
const BY_NAME: Record<string, () => Extension[]> = {
  dockerfile: () => [legacy(dockerFile)],
}

function parts(path: string): { name: string; ext: string } {
  const name = (path.split(/[\\/]/).pop() ?? path)
  const dot = name.lastIndexOf('.')
  return { name: name.toLowerCase(), ext: dot > 0 ? name.slice(dot + 1).toLowerCase() : '' }
}

export function langExtensions(path: string): Extension[] {
  const { name, ext } = parts(path)
  if (BY_NAME[name]) return BY_NAME[name]!()
  if (BY_EXT[ext]) return BY_EXT[ext]!()
  return []
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/fileLang.test.ts`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add packages/web/package.json pnpm-lock.yaml packages/web/src/components/fileLang.ts packages/web/src/components/fileLang.test.ts
git commit -m "feat(web): I3 CodeMirror deps + fileLang (ext→language, shell/cmd/properties/txt)"
```

---

## Task 9: CodeEditor.tsx — CodeMirror 薄封装

**Files:** Create `packages/web/src/components/CodeEditor.tsx`

> 无独立单测（CodeMirror 在 jsdom 难渲染；行为由浏览器阶段 Task 15 验，逻辑由被 mock 后的 FilesPanel 测）。靠 typecheck + 浏览器守护。

- [ ] **Step 1: 写实现**

```tsx
// packages/web/src/components/CodeEditor.tsx
import { useMemo } from 'react'
import CodeMirror from '@uiw/react-codemirror'
import { keymap } from '@codemirror/view'
import { Prec } from '@codemirror/state'
import { langExtensions } from './fileLang.js'
import { isDark } from '../theme.js'

/**
 * Thin CodeMirror wrapper: controlled value, language picked from the path, Ctrl/Cmd+S → onSave.
 * The only place CodeMirror is imported — swapping it out (e.g. back to a textarea) touches just here.
 */
export function CodeEditor({ path, value, onChange, onSave }: {
  path: string
  value: string
  onChange: (v: string) => void
  onSave: () => void
}) {
  const extensions = useMemo(() => [
    // High precedence so Mod-s beats any default binding; preventDefault stops the browser Save dialog.
    Prec.highest(keymap.of([{ key: 'Mod-s', run: () => { onSave(); return true }, preventDefault: true }])),
    ...langExtensions(path),
  ], [path, onSave])
  return (
    <CodeMirror
      className="code-editor"
      value={value}
      theme={isDark() ? 'dark' : 'light'}
      extensions={extensions}
      onChange={onChange}
      basicSetup={{ lineNumbers: true, highlightActiveLine: true, foldGutter: false }}
    />
  )
}
```
> 若 `../theme.js` 没有导出 `isDark`：改用读取 `document.documentElement.getAttribute('data-theme') === 'dark'`（内联一个小函数），不新增依赖。实现时先 `grep isDark packages/web/src/theme.ts` 确认；无则内联。

- [ ] **Step 2: 类型检查**

Run: `pnpm -F @zuse/web typecheck`
Expected: 通过（`@uiw/react-codemirror` 默认导出接受 `theme: 'dark'|'light'`）

- [ ] **Step 3: 提交**

```bash
git add packages/web/src/components/CodeEditor.tsx
git commit -m "feat(web): I3 CodeEditor — CodeMirror wrapper (language + Mod-s save + theme)"
```

---

## Task 10: manageApi — writeFile / deleteFile / rawFileUrl

**Files:** Modify `packages/web/src/state/manageApi.ts`

> `request()` 在非 ok 时抛通用错、丢状态码，无法区分 409。故 `writeFile` 直接用 `fetch` 判状态。无独立单测（薄 fetch 包装，与既有 listDir/readFilePreview 一致）；由 FilesPanel 注入桩覆盖 + 浏览器验。

- [ ] **Step 1: 写实现** — 在 `readFilePreview` 之后加：

```ts
import type { WriteFileResult } from '@zuse/protocol' // 合并进顶部已有的 @zuse/protocol import

/** Thrown by writeFile on a 409 (file changed on disk since load) so the UI can offer force-overwrite. */
export class FileConflictError extends Error {
  constructor() { super('file changed on disk'); this.name = 'FileConflictError' }
}

/** PUT /api/files/content — write (edit or create). 409 → FileConflictError. Throws on other non-ok. */
export async function writeFile(path: string, content: string, cwd?: string, opts: { expectMtimeMs?: number; force?: boolean } = {}): Promise<WriteFileResult> {
  const qs = new URLSearchParams()
  if (cwd) qs.set('cwd', cwd)
  const body = JSON.stringify({ path, content, expectMtimeMs: opts.expectMtimeMs, force: opts.force })
  const r = await fetch('/api/files/content' + (qs.toString() ? '?' + qs.toString() : ''), {
    method: 'PUT', credentials: 'same-origin', headers: JSON_HEADERS, body,
  })
  if (r.status === 409) throw new FileConflictError()
  if (!r.ok) throw new Error('write file failed: ' + r.status)
  return (await r.json()) as WriteFileResult
}

/** DELETE /api/files/content?path=…&cwd=… — delete a file. */
export async function deleteFile(path: string, cwd?: string): Promise<void> {
  const qs = new URLSearchParams({ path })
  if (cwd) qs.set('cwd', cwd)
  await request('/api/files/content?' + qs.toString(), { method: 'DELETE' }, 'delete file')
}

/** URL for the raw byte endpoint — used as <img>/<iframe> src and download href (same-origin, cookie sent). */
export function rawFileUrl(path: string, cwd?: string): string {
  const qs = new URLSearchParams({ path })
  if (cwd) qs.set('cwd', cwd)
  return '/api/files/raw?' + qs.toString()
}
```
> 顶部 `import type { … } from '@zuse/protocol'` 追加 `WriteFileResult`（与现有 DirListing/FilePreview 并列，别新增一行 import）。

- [ ] **Step 2: 类型检查**

Run: `pnpm -F @zuse/web typecheck`
Expected: 通过

- [ ] **Step 3: 提交**

```bash
git add packages/web/src/state/manageApi.ts
git commit -m "feat(web): I3 manageApi writeFile/deleteFile/rawFileUrl + FileConflictError"
```

---

## Task 11: FilesPanel — 视图分流（image / pdf / other 只读）

**Files:** Modify `packages/web/src/components/FilesPanel.tsx`, Test `FilesPanel.test.tsx`

> 先做只读分流（不含编辑器；编辑器 Task 12）。新增注入 prop `rawUrl`。测试里 mock CodeEditor 到 textarea（Task 12 才需要），本任务只测 image/pdf/other/binary/truncated。

- [ ] **Step 1: 写失败测试**（追加到 `FilesPanel.test.tsx`）

先在 `setup()` 的 props 里补默认注入（改 `setup` 与默认桩）：
```tsx
const defaultWrite = vi.fn(async (path: string) => ({ path, size: 1, mtimeMs: 2 }))
const defaultDelete = vi.fn(async () => {})
const defaultRawUrl = (path: string) => '/raw/' + path
// setup(): props 增加 writeFile/deleteFile/rawUrl 默认值
```
（把 `setup` 的 `props` 扩为包含 `writeFile: over.writeFile ?? defaultWrite, deleteFile: over.deleteFile ?? defaultDelete, rawUrl: over.rawUrl ?? defaultRawUrl`，并给 root 树加图片/pdf/other 文件项。）

在 root 的 entries 里加：
```tsx
{ name: 'pic.png', path: 'pic.png', type: 'file' },
{ name: 'doc.pdf', path: 'doc.pdf', type: 'file' },
{ name: 'sheet.xlsx', path: 'sheet.xlsx', type: 'file' },
```
用例：
```tsx
it('renders an <img> for an image file (no content fetch)', async () => {
  const loadFile = vi.fn()
  setup({ loadFile })
  fireEvent.click(await screen.findByText('pic.png'))
  const img = await screen.findByRole('img')
  expect(img.getAttribute('src')).toBe('/raw/pic.png')
  expect(loadFile).not.toHaveBeenCalled()
})

it('renders an <iframe> for a pdf (no content fetch)', async () => {
  const loadFile = vi.fn()
  setup({ loadFile })
  fireEvent.click(await screen.findByText('doc.pdf'))
  const frame = await screen.findByTitle('doc.pdf')
  expect(frame.getAttribute('src')).toBe('/raw/doc.pdf')
  expect(loadFile).not.toHaveBeenCalled()
})

it('shows "cannot display" + download for an unsupported type', async () => {
  const loadFile = vi.fn()
  setup({ loadFile })
  fireEvent.click(await screen.findByText('sheet.xlsx'))
  expect(await screen.findByText(/无法展示/)).toBeInTheDocument()
  expect(screen.getByText('下载').getAttribute('href')).toBe('/raw/sheet.xlsx')
})

it('shows "cannot display" + download for a truncated/binary text file', async () => {
  const loadFile = vi.fn(async (path: string) => ({ path, content: '', truncated: false, binary: true, size: 9, mtimeMs: 1 }))
  setup({ loadFile })
  fireEvent.click(await screen.findByText('readme.md'))
  expect(await screen.findByText(/无法展示/)).toBeInTheDocument()
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx`
Expected: FAIL（Props 无 rawUrl；无 img/iframe 分流）

- [ ] **Step 3: 写实现** — 更新 `FilesPanel.tsx`：

Props 与签名：
```tsx
import type { DirListing, FileEntry, FilePreview } from '@zuse/protocol'
import { classify } from './fileView.js'

interface Props {
  active: boolean
  loadDir: (dir: string) => Promise<DirListing>
  loadFile: (path: string) => Promise<FilePreview>
  writeFile: (path: string, content: string, opts?: { expectMtimeMs?: number; force?: boolean }) => Promise<{ path: string; size: number; mtimeMs: number }>
  deleteFile: (path: string) => Promise<void>
  rawUrl: (path: string) => string
}
export function FilesPanel({ active, loadDir, loadFile, writeFile, deleteFile, rawUrl }: Props) {
```
`openFile` 改为按 classify 分流（image/pdf 不拉 content）：
```tsx
  const [view, setView] = useState<'text' | 'image' | 'pdf' | 'other'>('text')
  const openFile = async (path: string) => {
    setSelected(path); setPreview(null); setError(null)
    const v = classify(path)
    setView(v)
    if (v !== 'text') { setPreviewLoading(false); return } // image/pdf/other render from rawUrl, no content fetch
    setPreviewLoading(true)
    try { setPreview(await loadFile(path)) }
    catch (e) { setError(e instanceof Error ? e.message : String(e)) }
    finally { setPreviewLoading(false) }
  }
```
预览区渲染分流（替换现有 `{selected ? (…)}` 的预览体）：
```tsx
      {selected ? (
        <div className="file-preview">
          <div className="file-preview-head" title={selected}>{selected}</div>
          {view === 'image' ? (
            <img className="file-img" src={rawUrl(selected)} alt={selected} />
          ) : view === 'pdf' ? (
            <iframe className="file-frame" src={rawUrl(selected)} title={selected} />
          ) : view === 'other' ? (
            <div className="file-cannot">无法展示此类型文件。<a href={rawUrl(selected)} download>下载</a></div>
          ) : previewLoading ? (
            <div className="mem-empty">加载中…</div>
          ) : preview ? (
            preview.binary || preview.truncated ? (
              <div className="file-cannot">{preview.binary ? '二进制文件，无法展示。' : '文件过大，无法编辑。'} <a href={rawUrl(selected)} download>下载</a></div>
            ) : (
              <pre className="file-preview-body">{stripAnsi(preview.content)}</pre>
            )
          ) : null}
        </div>
      ) : null}
```
> 本任务文本仍是只读 `<pre>`（Task 12 换成编辑器）。

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx`
Expected: PASS（新 4 条 + 既有；既有"二进制"用例文案从"二进制文件（…字节）"变为"二进制文件，无法展示。"，同步改既有断言 `findByText(/二进制文件/)` 仍匹配 → 不需改）

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/FilesPanel.tsx packages/web/src/components/FilesPanel.test.tsx
git commit -m "feat(web): I3 FilesPanel view routing — image/pdf/other read-only preview"
```

---

## Task 12: FilesPanel — 文本编辑器 + 保存 + 冲突

**Files:** Modify `packages/web/src/components/FilesPanel.tsx`, Test `FilesPanel.test.tsx`

> 测试 mock `CodeEditor` 为一个受控 `<textarea>`（CodeMirror 在 jsdom 不可靠），只测面板的 buffer/dirty/save/conflict 逻辑。
>
> **重要（既有测试迁移）**：本任务把文本分支从只读 `<pre>`（用 `stripAnsi`）换成**可编辑 CodeEditor，显示原始内容**（可编辑文本不能 strip ANSI，否则保存会篡改文件）。因此：
> - `stripAnsi` 在面板里不再被使用 → **删除 `FilesPanel.tsx` 里的 `stripAnsi` 函数与其 import 用法**，并删除 `FilesPanel.test.tsx` 里的 `describe('stripAnsi', …)` 块与 `it('strips ANSI codes from the preview body', …)` 用例（行为已随只读 `<pre>` 一并移除）。
> - 既有 `it('clicking a file fetches and shows its preview', …)` 断言 `findByText('export const a = 1')` 失效（内容进了 textarea 的 value，非文本节点）→ 改为断言编辑器 value（见下）。

- [ ] **Step 1: 迁移既有测试 + 写新失败测试** — 文件顶部加 mock；删除上面列出的 stripAnsi 相关测试；把 "clicking a file fetches and shows its preview" 改为：

```tsx
it('clicking a text file loads it into the editor', async () => {
  const loadFile = vi.fn(async (path: string) => ({ path, content: 'export const a = 1', truncated: false, binary: false, size: 18, mtimeMs: 1 }))
  setup({ loadFile })
  fireEvent.click(await screen.findByText('readme.md'))
  await waitFor(() => expect(loadFile).toHaveBeenCalledWith('readme.md'))
  expect((await screen.findByLabelText('editor') as HTMLTextAreaElement).value).toBe('export const a = 1')
})
```

再追加新用例：

```tsx
import { FileConflictError } from '../state/manageApi.js'

// CodeMirror can't render in jsdom — mock the editor to a plain textarea that drives onChange/onSave.
vi.mock('./CodeEditor.js', () => ({
  CodeEditor: ({ value, onChange, onSave }: { value: string; onChange: (v: string) => void; onSave: () => void }) => (
    <textarea aria-label="editor" value={value} onChange={(e) => onChange(e.target.value)}
      onKeyDown={(e) => { if ((e.ctrlKey || e.metaKey) && e.key === 's') { e.preventDefault(); onSave() } }} />
  ),
}))
```
用例：
```tsx
it('edits text in the editor and saves via writeFile (clears dirty)', async () => {
  const writeFile = vi.fn(async (path: string) => ({ path, size: 3, mtimeMs: 99 }))
  const loadFile = vi.fn(async (path: string) => ({ path, content: 'old', truncated: false, binary: false, size: 3, mtimeMs: 5 }))
  setup({ loadFile, writeFile })
  fireEvent.click(await screen.findByText('readme.md'))
  const ta = await screen.findByLabelText('editor')
  fireEvent.change(ta, { target: { value: 'new' } })
  fireEvent.click(screen.getByText('保存'))
  await waitFor(() => expect(writeFile).toHaveBeenCalledWith('readme.md', 'new', { expectMtimeMs: 5 }))
})

it('Ctrl+S saves', async () => {
  const writeFile = vi.fn(async (path: string) => ({ path, size: 1, mtimeMs: 7 }))
  const loadFile = vi.fn(async (path: string) => ({ path, content: 'x', truncated: false, binary: false, size: 1, mtimeMs: 5 }))
  setup({ loadFile, writeFile })
  fireEvent.click(await screen.findByText('readme.md'))
  const ta = await screen.findByLabelText('editor')
  fireEvent.change(ta, { target: { value: 'y' } })
  fireEvent.keyDown(ta, { key: 's', ctrlKey: true })
  await waitFor(() => expect(writeFile).toHaveBeenCalled())
})

it('on a 409 conflict, confirming overwrite re-saves with force', async () => {
  const loadFile = vi.fn(async (path: string) => ({ path, content: 'x', truncated: false, binary: false, size: 1, mtimeMs: 5 }))
  const writeFile = vi.fn()
    .mockRejectedValueOnce(new FileConflictError())
    .mockResolvedValueOnce({ path: 'readme.md', size: 1, mtimeMs: 9 })
  setup({ loadFile, writeFile })
  fireEvent.click(await screen.findByText('readme.md'))
  fireEvent.change(await screen.findByLabelText('editor'), { target: { value: 'y' } })
  fireEvent.click(screen.getByText('保存'))
  fireEvent.click(await screen.findByText('覆盖'))
  await waitFor(() => expect(writeFile).toHaveBeenLastCalledWith('readme.md', 'y', { force: true }))
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx`
Expected: FAIL（无编辑器/保存/覆盖 UI）

- [ ] **Step 3: 写实现** — `FilesPanel.tsx`：

删除 `stripAnsi` 函数（M7 遗留，行 4–11）及其在 `<pre>` 里的调用（文本分支马上改成编辑器，不再需要）。import 编辑器与冲突错误：
```tsx
import { CodeEditor } from './CodeEditor.js'
import { FileConflictError } from '../state/manageApi.js'
```
加编辑状态（组件内）：
```tsx
  const [buffer, setBuffer] = useState('')        // editor content
  const [baseMtime, setBaseMtime] = useState<number | null>(null)
  const [saving, setSaving] = useState(false)
  const [conflict, setConflict] = useState(false) // a 409 awaiting overwrite confirm
  const dirty = preview != null && !preview.binary && !preview.truncated && buffer !== preview.content
```
`openFile` 的 text 分支成功后初始化 buffer：在 `setPreview(p)` 处改为：
```tsx
      const p = await loadFile(path)
      setPreview(p); setBuffer(p.content); setBaseMtime(p.mtimeMs); setConflict(false)
```
保存函数：
```tsx
  const doSave = async (force = false) => {
    if (!selected) return
    setSaving(true); setError(null)
    try {
      const r = await writeFile(selected, buffer, force ? { force: true } : { expectMtimeMs: baseMtime ?? undefined })
      setBaseMtime(r.mtimeMs); setConflict(false)
      setPreview((prev) => (prev ? { ...prev, content: buffer, mtimeMs: r.mtimeMs } : prev)) // clear dirty
    } catch (e) {
      if (e instanceof FileConflictError) setConflict(true)
      else setError(e instanceof Error ? e.message : String(e))
    } finally { setSaving(false) }
  }
```
文本预览体（Task 11 的 `<pre>` 分支）替换为编辑器 + 工具条：
```tsx
            ) : (
              <div className="file-editor">
                <div className="file-editbar">
                  <button className={'file-save' + (dirty ? ' dirty' : '')} disabled={!dirty || saving} onClick={() => void doSave()}>保存</button>
                  {conflict ? (
                    <span className="file-conflict">文件已在磁盘变更。<button onClick={() => void doSave(true)}>覆盖</button></span>
                  ) : null}
                  {error ? <span className="file-cannot">{error}</span> : null}
                </div>
                <CodeEditor path={selected} value={buffer} onChange={setBuffer} onSave={() => void doSave()} />
              </div>
            )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/FilesPanel.tsx packages/web/src/components/FilesPanel.test.tsx
git commit -m "feat(web): I3 FilesPanel text editor — save (Ctrl/Cmd+S), dirty, 409 overwrite"
```

---

## Task 13: FilesPanel — 新建 + 删除

**Files:** Modify `packages/web/src/components/FilesPanel.tsx`, Test `FilesPanel.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
it('creates a new file via writeFile and opens it', async () => {
  const writeFile = vi.fn(async (path: string) => ({ path, size: 0, mtimeMs: 1 }))
  const loadFile = vi.fn(async (path: string) => ({ path, content: '', truncated: false, binary: false, size: 0, mtimeMs: 1 }))
  setup({ writeFile, loadFile })
  await screen.findByText('src')
  fireEvent.click(screen.getByText('＋ 新建文件'))
  fireEvent.change(screen.getByPlaceholderText('相对路径，如 src/new.ts'), { target: { value: 'src/new.ts' } })
  fireEvent.click(screen.getByText('创建'))
  await waitFor(() => expect(writeFile).toHaveBeenCalledWith('src/new.ts', ''))
})

it('deletes a file after inline confirm', async () => {
  const deleteFile = vi.fn(async () => {})
  const loadFile = vi.fn(async (path: string) => ({ path, content: 'x', truncated: false, binary: false, size: 1, mtimeMs: 1 }))
  setup({ deleteFile, loadFile })
  fireEvent.click(await screen.findByText('readme.md'))
  fireEvent.click(await screen.findByTitle('删除文件'))   // trash affordance in the preview head
  fireEvent.click(await screen.findByTitle('确认删除'))   // ✓
  await waitFor(() => expect(deleteFile).toHaveBeenCalledWith('readme.md'))
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx`
Expected: FAIL

- [ ] **Step 3: 写实现** — `FilesPanel.tsx`：

新建状态 + 处理：
```tsx
  const [creating, setCreating] = useState(false)
  const [newPath, setNewPath] = useState('')
  const [delConfirm, setDelConfirm] = useState(false)
  const doCreate = async () => {
    const p = newPath.trim()
    if (!p) return
    setError(null)
    try {
      await writeFile(p, '')
      setCreating(false); setNewPath('')
      const dir = p.includes('/') ? p.slice(0, p.lastIndexOf('/')) : ''
      await fetchDir(dir)                 // refresh the containing directory
      await openFile(p)                   // open the new file
    } catch (e) { setError(e instanceof Error ? e.message : String(e)) }
  }
  const doDelete = async () => {
    if (!selected) return
    try {
      await deleteFile(selected)
      setChildren((m) => { const n = new Map(m); for (const [d, es] of n) n.set(d, es.filter((e) => e.path !== selected)); return n })
      setSelected(null); setPreview(null); setDelConfirm(false)
    } catch (e) { setError(e instanceof Error ? e.message : String(e)) }
  }
```
树上方"＋ 新建文件"（放在 `file-root` 之后、`file-tree` 之前）：
```tsx
      <div className="file-actions">
        {creating ? (
          <span className="file-newrow">
            <input className="session-rename-input" placeholder="相对路径，如 src/new.ts" value={newPath}
              autoFocus onChange={(e) => setNewPath(e.target.value)}
              onKeyDown={(e) => { if (e.key === 'Enter') void doCreate(); if (e.key === 'Escape') { setCreating(false); setNewPath('') } }} />
            <button className="file-new-ok" onClick={() => void doCreate()}>创建</button>
            <button className="file-new-cancel" onClick={() => { setCreating(false); setNewPath('') }}>取消</button>
          </span>
        ) : (
          <button className="file-new-btn" onClick={() => setCreating(true)}>＋ 新建文件</button>
        )}
      </div>
```
预览头加删除入口（在 `file-preview-head` 里）：
```tsx
          <div className="file-preview-head" title={selected}>
            <span className="file-preview-path">{selected}</span>
            {delConfirm ? (
              <span className="file-del-confirm">
                <button title="确认删除" onClick={() => void doDelete()}>✓</button>
                <button title="取消删除" onClick={() => setDelConfirm(false)}>✕</button>
              </span>
            ) : (
              <button className="file-del" title="删除文件" onClick={() => setDelConfirm(true)}>🗑</button>
            )}
          </div>
```
（切文件时复位 `delConfirm`：在 `openFile` 开头加 `setDelConfirm(false)`。）

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/FilesPanel.tsx packages/web/src/components/FilesPanel.test.tsx
git commit -m "feat(web): I3 FilesPanel create + delete (inline confirm)"
```

---

## Task 14: FilesPanel — 未保存守卫 + ManageDrawer 接线 + 样式

**Files:** Modify `FilesPanel.tsx`, `FilesPanel.test.tsx`, `ManageDrawer.tsx`, `styles.css`

- [ ] **Step 1: 写失败测试**（未保存守卫）

```tsx
it('warns before leaving a dirty editor for another file', async () => {
  const loadFile = vi.fn(async (path: string) => ({ path, content: 'x', truncated: false, binary: false, size: 1, mtimeMs: 1 }))
  const confirmSpy = vi.spyOn(window, 'confirm').mockReturnValue(false)
  setup({ loadFile })
  fireEvent.click(await screen.findByText('readme.md'))
  fireEvent.change(await screen.findByLabelText('editor'), { target: { value: 'dirty!' } })
  fireEvent.click(screen.getByText('src'))          // try to navigate away (expand dir)
  fireEvent.click(await screen.findByText('a.ts'))  // click another file
  expect(confirmSpy).toHaveBeenCalled()
  confirmSpy.mockRestore()
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx`
Expected: FAIL（无守卫）

- [ ] **Step 3: 写实现（守卫）** — `openFile` 开头加：

```tsx
    if (dirty && !window.confirm('有未保存的修改，放弃并切换？')) return
```
（放在 `openFile` 的最前面，`setSelected` 之前。）

- [ ] **Step 4: ManageDrawer 接线** — `ManageDrawer.tsx` 的 `FilesContainer`：

顶部 import 补：
```tsx
import { listDir, readFilePreview, writeFile, deleteFile, rawFileUrl } from '../state/manageApi.js'
```
`FilesContainer` 的 `<FilesPanel …>` 增补三个注入：
```tsx
    <FilesPanel
      key={cwd ?? ''}
      active={active}
      loadDir={(dir) => listDir(dir, cwd)}
      loadFile={(path) => readFilePreview(path, cwd)}
      writeFile={(path, content, opts) => writeFile(path, content, cwd, opts)}
      deleteFile={(path) => deleteFile(path, cwd)}
      rawUrl={(path) => rawFileUrl(path, cwd)}
    />
```

- [ ] **Step 5: 样式** — `styles.css` 末尾（或 file-* 规则附近）加：

```css
.file-actions { padding: 4px 6px 8px; }
.file-new-btn { background: transparent; border: 1px solid var(--line); color: var(--muted); font-size: 12.5px; padding: 5px 10px; border-radius: 8px; cursor: pointer; }
.file-new-btn:hover { background: var(--surface-2); color: var(--text); }
.file-newrow { display: flex; gap: 6px; align-items: center; }
.file-new-ok, .file-new-cancel { font-size: 12px; padding: 5px 9px; border-radius: 7px; border: 1px solid var(--line); cursor: pointer; }
.file-new-ok { background: var(--accent); color: var(--on-accent); border-color: transparent; }
.file-new-cancel { background: transparent; color: var(--muted); }
.file-preview-head { display: flex; align-items: center; gap: 8px; }
.file-preview-path { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.file-del, .file-del-confirm button { background: transparent; border: 0; cursor: pointer; font-size: 13px; padding: 2px 5px; border-radius: 6px; color: var(--muted); }
.file-del:hover { color: var(--bad); background: var(--surface-2); }
.file-del-confirm { display: inline-flex; gap: 2px; }
.file-editor { display: flex; flex-direction: column; gap: 6px; }
.file-editbar { display: flex; align-items: center; gap: 10px; }
.file-save { font-size: 12.5px; padding: 5px 12px; border-radius: 8px; border: 1px solid var(--line); background: transparent; color: var(--muted); cursor: pointer; }
.file-save.dirty { background: var(--accent); color: var(--on-accent); border-color: transparent; }
.file-save:disabled { opacity: .5; cursor: default; }
.file-conflict { font-size: 12px; color: var(--warn); display: inline-flex; gap: 6px; align-items: center; }
.file-conflict button { font-size: 12px; padding: 3px 9px; border-radius: 6px; border: 1px solid var(--warn); background: transparent; color: var(--warn); cursor: pointer; }
.file-cannot { color: var(--muted); font-size: 13px; padding: 8px 4px; }
.file-cannot a, .file-conflict a { color: var(--accent); }
.file-img { max-width: 100%; max-height: 60vh; object-fit: contain; display: block; border-radius: 8px; }
.file-frame { width: 100%; height: 60vh; border: 1px solid var(--line); border-radius: 8px; background: #fff; }
.code-editor { border: 1px solid var(--line); border-radius: 8px; overflow: hidden; max-height: 60vh; }
.code-editor .cm-editor { max-height: 60vh; }
```

- [ ] **Step 6: 跑全量 web 测试 + typecheck**

Run: `pnpm -F @zuse/web exec vitest run src/components/FilesPanel.test.tsx` → PASS
Run: `pnpm -F @zuse/web typecheck` → 通过

- [ ] **Step 7: 提交**

```bash
git add packages/web/src/components/FilesPanel.tsx packages/web/src/components/FilesPanel.test.tsx packages/web/src/components/ManageDrawer.tsx packages/web/src/styles.css
git commit -m "feat(web): I3 unsaved-switch guard + ManageDrawer wiring + files panel styles"
```

---

## Task 15: 全量测试 + 构建 + 浏览器实测

**Files:** —（验证任务）

- [ ] **Step 1: 全量**

Run: `pnpm -F @zouyj/zuse-server test` → 绿；`pnpm -F @zuse/web test` → 绿；两包 typecheck → 通过。

- [ ] **Step 2: 构建 + 重启 daemon**

Run: `pnpm -F @zuse/web build`；重启 daemon（`/restart` skill 或杀 4180 PID 后 `pnpm exec tsx packages/server/src/bin.ts` 后台起——server 有改动，必须重启）。

- [ ] **Step 3: 浏览器实测**（Playwright MCP，127.0.0.1:4180 → 管理抽屉 → 文件）

1. 打开一个 `.ts`/`.md` → CodeMirror 编辑器出现、语法高亮；改内容 → "保存"高亮 → 点保存 / Ctrl+S → 落盘（可 `Get-Content` 核对文件真变了）。
2. 打开一张图片 → `<img>` 显示；打开一个 PDF → `<iframe>` 内置查看器渲染。
3. 打开一个 `.xlsx`/`.zip` → "无法展示 + 下载"，点下载能下。
4. ＋ 新建文件 → 输入路径创建 → 进编辑器；删除 → 内联确认 → 文件消失。
5. 编辑器有脏改动时点另一文件 → 弹"放弃并切换？"。
6. 明暗主题切换，编辑器主题跟随。

- [ ] **Step 4: 无独立提交**（代码已在前序任务提交；本任务仅验证。若浏览器暴露问题，回到相应任务修 + 补测。）

---

## Self-Review 记录

- **spec 覆盖**：§3.1 类型→T1；§3.2 write/remove/statFile/FileChangedError/mtimeMs→T2/T3/T4；§3.3 PUT/DELETE→T5、GET raw→T6；§3.4 MIME→T4；§4.1 CM 依赖+语言→T8、CodeEditor→T9；§4.2 manageApi→T10；§4.3 classify→T7、视图分流→T11；§4.4 编辑器/保存/冲突→T12；§4.5 新建/删除→T13；§4.6 未保存守卫→T14；§4.7 接线→T14；§6 测试分布在 T2–T14；§5 边界（越界 403 / 目录 / 截断二进制 / raw 413 / 冲突 409 / 父目录缺失）分别落在 T2–T6、T11。
- **偏差**：`PathOutsideRootError` 映射为 **403**（沿用现有 `/api/files` GET 的约定，spec §3.3 写的 400 以此为准修正）。
- **类型一致**：`WriteFileResult{path,size,mtimeMs}` T1↔T2↔T10↔T11(inject 签名)；`FileChangedError`(server) T2↔T5；`FileConflictError`(web) T10↔T12；`classify` 返回联合 T7↔T11；`langExtensions` T8↔T9。
- **jsdom 限制**：CodeMirror 单测不可靠 → T12/T13/T14 mock `./CodeEditor.js` 为 textarea；真编辑器由 T15 浏览器验。
- **依赖网络**：T8 `pnpm add` 需联网；离线则 BLOCKED 停下报告。
```
