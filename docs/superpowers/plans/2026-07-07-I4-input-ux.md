# I4 — Web 输入框 UX 增强 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 web 输入框加输入历史（↑/↓）、`/` 命令自动完成菜单、Esc 停止，并把发送/停止按钮换成 SVG 图标。

**Architecture:** 纯前端改动（`packages/web`）。命令描述符统一为 `SlashCommand { name, desc, run(ctx) }`，命令表 + 过滤器抽到 `commands.ts`（纯函数，可单测）。`Composer` 拿到 `history` 数组、`commands` 列表与 `onRunCommand` 回调，自己管命令菜单/历史游标/键盘裁决；`Shell` 持 `historyBySession`、组装 `CommandContext`、把命令 run 接到已有能力（send/newSession/openPanel/focusHistorySearch/openDirPicker）。`DirPicker` 加一个 `useImperativeHandle` 的 `open()` 供 `/work` 触发。

**Tech Stack:** React 19, TypeScript, Vitest + @testing-library/react（jsdom），CSS 在 `packages/web/src/styles.css`。

**测试命令（全程通用）：** `pnpm -F @zuse/web exec vitest run <file>`；类型检查 `pnpm -F @zuse/web typecheck`。

---

## 文件结构

- **新建** `packages/web/src/components/commands.ts` — `SlashCommand` / `CommandContext` 类型、命令表 `SLASH_COMMANDS`、纯函数 `filterCommands(input, commands)`。
- **新建** `packages/web/src/components/commands.test.ts` — `filterCommands` 单测。
- **改** `packages/web/src/components/Composer.tsx` — 新增 props（history/commands/onRunCommand）、命令菜单、历史游标、键盘裁决、SVG 图标按钮。
- **改** `packages/web/src/components/Composer.test.tsx` — 补测试；修停止按钮改图标后的既有断言。
- **改** `packages/web/src/components/DirPicker.tsx` — `forwardRef` + `useImperativeHandle({ open })`。
- **改** `packages/web/src/components/Header.tsx` — 透传一个 `dirPickerRef` 到 DirPicker。
- **改** `packages/web/src/components/Shell.tsx` — `historyBySession`、组装 ctx、`onSend` 入历史、把 `history`/`commands`/`onRunCommand` 传给 Composer、删除旧 `SLASH_COMMANDS`。
- **改** `packages/web/src/styles.css` — 命令菜单浮层样式 + 发送/停止按钮图标态（禁用/hover/active）。

---

## Task 1: 命令模型 —— `commands.ts` 类型、命令表与过滤器

**Files:**
- Create: `packages/web/src/components/commands.ts`
- Test: `packages/web/src/components/commands.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
// packages/web/src/components/commands.test.ts
import { describe, it, expect, vi } from 'vitest'
import { SLASH_COMMANDS, filterCommands } from './commands.js'

describe('filterCommands', () => {
  it('returns [] when input does not start with a slash', () => {
    expect(filterCommands('hello', SLASH_COMMANDS)).toEqual([])
    expect(filterCommands('', SLASH_COMMANDS)).toEqual([])
  })

  it('returns all commands for a bare slash', () => {
    expect(filterCommands('/', SLASH_COMMANDS)).toEqual(SLASH_COMMANDS)
  })

  it('prefix-matches command names case-insensitively', () => {
    const hits = filterCommands('/CO', SLASH_COMMANDS)
    expect(hits.map((c) => c.name)).toEqual(['/compact'])
  })

  it('returns [] when nothing matches the prefix', () => {
    expect(filterCommands('/zzz', SLASH_COMMANDS)).toEqual([])
  })
})

describe('SLASH_COMMANDS table', () => {
  it('runs /compact as a compact uplink', () => {
    const ctx = { send: vi.fn(), newSession: vi.fn(), openPanel: vi.fn(), focusHistorySearch: vi.fn(), showHelp: vi.fn(), openDirPicker: vi.fn() }
    SLASH_COMMANDS.find((c) => c.name === '/compact')!.run(ctx)
    expect(ctx.send).toHaveBeenCalledWith({ type: 'compact' })
  })

  it('runs /clear as newSession and /files as openPanel(files)', () => {
    const ctx = { send: vi.fn(), newSession: vi.fn(), openPanel: vi.fn(), focusHistorySearch: vi.fn(), showHelp: vi.fn(), openDirPicker: vi.fn() }
    SLASH_COMMANDS.find((c) => c.name === '/clear')!.run(ctx)
    expect(ctx.newSession).toHaveBeenCalled()
    SLASH_COMMANDS.find((c) => c.name === '/files')!.run(ctx)
    expect(ctx.openPanel).toHaveBeenCalledWith('files')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/commands.test.ts`
Expected: FAIL（`Cannot find module './commands.js'`）

- [ ] **Step 3: 写实现**

```ts
// packages/web/src/components/commands.ts
import type { ClientMessage } from '@zuse/protocol'
import type { ManagePanel } from './ManageDrawer.js'

/** Capabilities a slash command may invoke. Shell builds a concrete ctx and passes it to run(). */
export interface CommandContext {
  send: (msg: ClientMessage) => void
  newSession: () => void
  openPanel: (panel: ManagePanel) => void
  focusHistorySearch: () => void
  showHelp: () => void
  openDirPicker: () => void
}

export interface SlashCommand {
  name: string // e.g. '/compact'
  desc: string // one-line menu description
  run: (ctx: CommandContext) => void
}

// Web slash commands. Two kinds share one shape: server commands call ctx.send(uplink); frontend
// commands drive existing UI (new session / open a manage panel / focus history search / open the
// dir picker). Adding a command = one entry here. Deliberately NOT included: /tools /revert /save —
// they have no clean web target (revert is a per-message icon, save is the share-selection flow).
export const SLASH_COMMANDS: SlashCommand[] = [
  { name: '/compact', desc: '压缩当前上下文', run: (c) => c.send({ type: 'compact' }) },
  { name: '/clear', desc: '新建会话（清空上下文）', run: (c) => c.newSession() },
  { name: '/help', desc: '列出所有命令', run: (c) => c.showHelp() },
  { name: '/memory', desc: '打开记忆管理', run: (c) => c.openPanel('memory') },
  { name: '/prompts', desc: '打开人设/提示词', run: (c) => c.openPanel('prompts') },
  { name: '/skills', desc: '打开技能管理', run: (c) => c.openPanel('skills') },
  { name: '/mcp', desc: '打开 MCP 管理', run: (c) => c.openPanel('mcp') },
  { name: '/usage', desc: '打开用量面板', run: (c) => c.openPanel('usage') },
  { name: '/files', desc: '打开文件浏览器', run: (c) => c.openPanel('files') },
  { name: '/history', desc: '搜索历史消息', run: (c) => c.focusHistorySearch() },
  { name: '/work', desc: '切换工作目录', run: (c) => c.openDirPicker() },
]

/** Menu candidates for the current input: prefix-match on name (case-insensitive) when input
 * starts with '/'; otherwise no menu. A bare '/' lists everything. */
export function filterCommands(input: string, commands: SlashCommand[]): SlashCommand[] {
  if (!input.startsWith('/')) return []
  const q = input.toLowerCase()
  return commands.filter((c) => c.name.toLowerCase().startsWith(q))
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/commands.test.ts`
Expected: PASS（全部 6 条）

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/commands.ts packages/web/src/components/commands.test.ts
git commit -m "feat(web): I4 slash-command model — table + filter (commands.ts)"
```

---

## Task 2: Composer 命令菜单 —— 渲染 + 过滤 + 鼠标点击执行

**Files:**
- Modify: `packages/web/src/components/Composer.tsx`
- Test: `packages/web/src/components/Composer.test.tsx`

> 先把 Composer 的 props 扩成新契约（向后兼容默认值），并渲染命令菜单浮层。键盘导航留到 Task 3。

- [ ] **Step 1: 写失败测试**（追加到 `Composer.test.tsx`）

```tsx
import { SLASH_COMMANDS } from './commands.js'

describe('Composer slash menu', () => {
  it('shows a filtered command menu while input starts with "/"', () => {
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} commands={SLASH_COMMANDS} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: '/co' } })
    expect(screen.getByText('/compact')).toBeInTheDocument()
    expect(screen.queryByText('/clear')).not.toBeInTheDocument()
  })

  it('hides the menu when input has no leading slash', () => {
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} commands={SLASH_COMMANDS} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: 'hello' } })
    expect(screen.queryByText('/compact')).not.toBeInTheDocument()
  })

  it('clicking a menu item runs it and clears input', () => {
    const onRunCommand = vi.fn()
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} commands={SLASH_COMMANDS} onRunCommand={onRunCommand} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: '/comp' } })
    fireEvent.click(screen.getByText('/compact'))
    expect(onRunCommand).toHaveBeenCalledWith(SLASH_COMMANDS.find((c) => c.name === '/compact'))
    expect(ta.value).toBe('')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: FAIL（Composer 不接受 `commands` prop / 无 `/compact` 文本）

- [ ] **Step 3: 写实现** —— 替换 `Composer.tsx` 全文

```tsx
import { useEffect, useRef, useState } from 'react'
import type { SlashCommand } from './commands.js'
import { filterCommands } from './commands.js'

interface ComposerProps {
  thinking: boolean
  onSend: (text: string) => void
  onStop: () => void
  history?: string[]
  commands?: SlashCommand[]
  onRunCommand?: (cmd: SlashCommand) => void
}

export function Composer({ thinking, onSend, onStop, history = [], commands = [], onRunCommand }: ComposerProps) {
  const [value, setValue] = useState('')
  const taRef = useRef<HTMLTextAreaElement>(null)
  // Command menu: candidate list derived from current input + a highlighted index.
  const menu = filterCommands(value, commands)
  const menuOpen = menu.length > 0
  const [menuIdx, setMenuIdx] = useState(0)
  // History cursor: null = editing a fresh draft; otherwise an index into `history` (0 = oldest).
  const [histIdx, setHistIdx] = useState<number | null>(null)

  useEffect(() => { taRef.current?.focus() }, [])
  useEffect(() => { if (!thinking) taRef.current?.focus() }, [thinking])
  // New session → fresh history array → reset the cursor.
  useEffect(() => { setHistIdx(null) }, [history])
  // Keep the highlight in range as the candidate list shrinks/grows.
  useEffect(() => { setMenuIdx(0) }, [value])

  function setText(v: string) {
    setValue(v)
    const ta = taRef.current
    if (ta) { ta.style.height = 'auto'; ta.style.height = Math.min(ta.scrollHeight, 168) + 'px' }
  }

  function submit() {
    const v = value.trim()
    if (!v) return
    // Sending is allowed even while `thinking`: Shell routes it to a mid-turn steer.
    onSend(v)
    setValue(''); setHistIdx(null)
    if (taRef.current) { taRef.current.style.height = 'auto'; taRef.current.focus() }
  }

  function runCommand(cmd: SlashCommand) {
    onRunCommand?.(cmd)
    setValue(''); setHistIdx(null)
    if (taRef.current) { taRef.current.style.height = 'auto'; taRef.current.focus() }
  }

  return (
    <div className="composer-wrap">
      {menuOpen ? (
        <ul className="slash-menu" role="listbox" aria-label="命令">
          {menu.map((c, i) => (
            <li
              key={c.name}
              role="option"
              aria-selected={i === menuIdx}
              className={'slash-item' + (i === menuIdx ? ' active' : '')}
              onMouseDown={(e) => e.preventDefault()} // keep textarea focus; run on click
              onClick={() => runCommand(c)}
            >
              <span className="slash-name">{c.name}</span>
              <span className="slash-desc">{c.desc}</span>
            </li>
          ))}
        </ul>
      ) : null}
      <div className="composer">
        <textarea
          ref={taRef}
          rows={1}
          placeholder={thinking ? '插入消息到当前回合…' : '给 zuse 发消息…'}
          value={value}
          onChange={(e) => { setText(e.target.value); setHistIdx(null) }}
          onKeyDown={(e) => {
            // !isComposing: don't treat the Enter that confirms an IME candidate as a send.
            if (e.nativeEvent.isComposing) return
            if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); submit() }
          }}
        />
        {thinking ? (
          <button className="ghost stop-btn" aria-label="停止" onClick={onStop}>
            <svg viewBox="0 0 24 24" width="16" height="16" fill="currentColor" aria-hidden="true"><rect x="6" y="6" width="12" height="12" rx="2.5" /></svg>
          </button>
        ) : null}
        <button className="send-btn" aria-label="发送消息" disabled={value.trim() === ''} onClick={submit}>
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
            <polyline points="9 10 4 15 9 20" />
            <path d="M20 4v7a4 4 0 0 1-4 4H4" />
          </svg>
        </button>
      </div>
    </div>
  )
}
```

> 注：本任务已顺带把按钮换成 SVG 图标 + 发送禁用态（Task 5 只补测试与 CSS/Esc 逻辑），并用 `onMouseDown`+`preventDefault` 让点击菜单项不抢走 textarea 焦点。

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: 新增 3 条 slash menu PASS。既有 "shows Stop and fires onStop" 会因按钮改图标（无 "停止" 文本）而 FAIL —— 下一步修。

- [ ] **Step 5: 修既有停止按钮断言**（`Composer.test.tsx`）

把
```tsx
    fireEvent.click(screen.getByText('停止'))
```
改为
```tsx
    fireEvent.click(screen.getByLabelText('停止'))
```

- [ ] **Step 6: 跑全文件确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: PASS（全部）

- [ ] **Step 7: 提交**

```bash
git add packages/web/src/components/Composer.tsx packages/web/src/components/Composer.test.tsx
git commit -m "feat(web): I4 slash-command menu in Composer + SVG icon buttons"
```

---

## Task 3: Composer 命令菜单键盘导航（↑/↓/Enter/Tab/Esc）

**Files:**
- Modify: `packages/web/src/components/Composer.tsx`
- Test: `packages/web/src/components/Composer.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
describe('Composer slash menu keyboard', () => {
  it('ArrowDown moves the highlight; Enter runs the highlighted command', () => {
    const onRunCommand = vi.fn()
    const onSend = vi.fn()
    render(<Composer thinking={false} onSend={onSend} onStop={() => {}} commands={SLASH_COMMANDS} onRunCommand={onRunCommand} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: '/' } })
    // first item is /compact; ArrowDown → /clear
    fireEvent.keyDown(ta, { key: 'ArrowDown' })
    fireEvent.keyDown(ta, { key: 'Enter' })
    expect(onRunCommand).toHaveBeenCalledWith(SLASH_COMMANDS.find((c) => c.name === '/clear'))
    expect(onSend).not.toHaveBeenCalled() // Enter picked a command, did NOT send
  })

  it('Tab runs the highlighted command', () => {
    const onRunCommand = vi.fn()
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} commands={SLASH_COMMANDS} onRunCommand={onRunCommand} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: '/comp' } })
    fireEvent.keyDown(ta, { key: 'Tab' })
    expect(onRunCommand).toHaveBeenCalledWith(SLASH_COMMANDS.find((c) => c.name === '/compact'))
  })

  it('Escape closes the menu without running or clearing input', () => {
    const onRunCommand = vi.fn()
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} commands={SLASH_COMMANDS} onRunCommand={onRunCommand} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: '/comp' } })
    fireEvent.keyDown(ta, { key: 'Escape' })
    expect(onRunCommand).not.toHaveBeenCalled()
    expect(ta.value).toBe('/comp')
    expect(screen.queryByText('/compact')).not.toBeInTheDocument() // menu closed
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: FAIL（当前 onKeyDown 不处理方向键/Tab/Esc；`/` 菜单不会因 Esc 关闭）

- [ ] **Step 3: 写实现** —— 在 Composer 加"菜单已被 Esc 关闭"状态 + 键盘分支

在组件顶部 state 处新增：
```tsx
  // Esc dismisses the menu even though input still starts with '/'. Cleared whenever the input changes.
  const [menuDismissed, setMenuDismissed] = useState(false)
```

把 `const menuOpen = menu.length > 0` 改为：
```tsx
  const menuOpen = menu.length > 0 && !menuDismissed
```

`onChange` 改为同时清 dismissed：
```tsx
          onChange={(e) => { setText(e.target.value); setHistIdx(null); setMenuDismissed(false) }}
```

`runCommand` 末尾追加 `setMenuDismissed(false)`（执行后复位）。

把 `onKeyDown` 整体替换为：
```tsx
          onKeyDown={(e) => {
            if (e.nativeEvent.isComposing) return
            // 1) Command menu open: arrows/enter/tab/esc drive the menu.
            if (menuOpen) {
              if (e.key === 'ArrowDown') { e.preventDefault(); setMenuIdx((i) => (i + 1) % menu.length); return }
              if (e.key === 'ArrowUp') { e.preventDefault(); setMenuIdx((i) => (i - 1 + menu.length) % menu.length); return }
              if (e.key === 'Enter' || e.key === 'Tab') { e.preventDefault(); runCommand(menu[menuIdx]!); return }
              if (e.key === 'Escape') { e.preventDefault(); setMenuDismissed(true); return }
            }
            // 2) Menu closed: Enter sends.
            if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); submit() }
          }}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: PASS（新增 3 条 + 既有全绿）

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/Composer.tsx packages/web/src/components/Composer.test.tsx
git commit -m "feat(web): I4 slash-menu keyboard nav (arrows/enter/tab/esc)"
```

---

## Task 4: Composer 输入历史（↑/↓，含多行边界）

**Files:**
- Modify: `packages/web/src/components/Composer.tsx`
- Test: `packages/web/src/components/Composer.test.tsx`

- [ ] **Step 1: 写失败测试**

```tsx
describe('Composer input history', () => {
  it('ArrowUp recalls the most recent history entry, then older', () => {
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} history={['first', 'second']} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.keyDown(ta, { key: 'ArrowUp' })
    expect(ta.value).toBe('second')
    fireEvent.keyDown(ta, { key: 'ArrowUp' })
    expect(ta.value).toBe('first')
    fireEvent.keyDown(ta, { key: 'ArrowUp' }) // clamp at oldest
    expect(ta.value).toBe('first')
  })

  it('ArrowDown walks back toward the draft and restores it', () => {
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} history={['first', 'second']} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: 'draft' } })
    fireEvent.keyDown(ta, { key: 'ArrowUp' }) // → 'second'
    fireEvent.keyDown(ta, { key: 'ArrowDown' }) // → back to 'draft'
    expect(ta.value).toBe('draft')
  })

  it('does not recall history when input starts with "/" (command-input state)', () => {
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} history={['first']} commands={SLASH_COMMANDS} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: '/comp' } })
    fireEvent.keyDown(ta, { key: 'Escape' }) // dismiss menu so it's not the menu handling arrows
    fireEvent.keyDown(ta, { key: 'ArrowUp' })
    expect(ta.value).toBe('/comp') // unchanged — no history recall
  })

  it('does not recall history on ArrowUp when caret is not on the first line (multiline)', () => {
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} history={['old']} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: 'line1\nline2' } })
    ta.selectionStart = ta.selectionEnd = ta.value.length // caret on last line
    fireEvent.keyDown(ta, { key: 'ArrowUp' })
    expect(ta.value).toBe('line1\nline2') // caret moves within textarea; no recall
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: FAIL（方向键当前不翻历史）

- [ ] **Step 3: 写实现** —— 加历史导航辅助 + onKeyDown 分支

在组件内（`runCommand` 之后）加：
```tsx
  // Caret-position helpers so multi-line editing keeps normal arrow behavior; history only kicks in
  // at the text boundary (caret on the first line for ↑, last line for ↓), shell-style.
  function caretOnFirstLine() {
    const ta = taRef.current
    if (!ta) return true
    return ta.value.slice(0, ta.selectionStart).indexOf('\n') === -1
  }
  function caretOnLastLine() {
    const ta = taRef.current
    if (!ta) return true
    return ta.value.slice(ta.selectionStart).indexOf('\n') === -1
  }
  const draftRef = useRef('')
  function recallPrev() {
    if (history.length === 0) return
    const next = histIdx === null ? history.length - 1 : Math.max(0, histIdx - 1)
    if (histIdx === null) draftRef.current = value // entering history: stash the draft
    setHistIdx(next); setText(history[next]!)
  }
  function recallNext() {
    if (histIdx === null) return
    const next = histIdx + 1
    if (next >= history.length) { setHistIdx(null); setText(draftRef.current); return } // back to draft
    setHistIdx(next); setText(history[next]!)
  }
```

在 `onKeyDown` 的"菜单关"分支里，`Enter` 分支之后追加历史处理（注意：input 以 `/` 开头时视为命令输入态，不翻历史）：
```tsx
            // 3) Menu closed, not in command-input state: arrows navigate input history.
            if (!value.startsWith('/')) {
              if (e.key === 'ArrowUp' && caretOnFirstLine()) { e.preventDefault(); recallPrev(); return }
              if (e.key === 'ArrowDown' && caretOnLastLine()) { e.preventDefault(); recallNext(); return }
            }
```

> 说明：`histIdx` 已在 `onChange` 里被复位为 `null`（用户编辑历史项后从最新重新开始），满足 spec §3.1"编辑后失效"。

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: PASS（新增 4 条 + 既有全绿）

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/Composer.tsx packages/web/src/components/Composer.test.tsx
git commit -m "feat(web): I4 per-session input history (up/down, multiline-aware)"
```

---

## Task 5: Esc 停止 + 按钮禁用态测试补齐

**Files:**
- Modify: `packages/web/src/components/Composer.tsx`
- Test: `packages/web/src/components/Composer.test.tsx`

> 按钮图标已在 Task 2 落地；本任务补 Esc-停止逻辑（菜单关 + thinking 时）和禁用态断言。

- [ ] **Step 1: 写失败测试**

```tsx
describe('Composer Esc-to-stop and disabled send', () => {
  it('Escape stops the turn while thinking and menu is closed', () => {
    const onStop = vi.fn()
    render(<Composer thinking={true} onSend={() => {}} onStop={onStop} />)
    const ta = screen.getByPlaceholderText('插入消息到当前回合…') as HTMLTextAreaElement
    fireEvent.keyDown(ta, { key: 'Escape' })
    expect(onStop).toHaveBeenCalled()
  })

  it('Escape does not stop when not thinking', () => {
    const onStop = vi.fn()
    render(<Composer thinking={false} onSend={() => {}} onStop={onStop} />)
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.keyDown(ta, { key: 'Escape' })
    expect(onStop).not.toHaveBeenCalled()
  })

  it('Escape closes the menu instead of stopping, even while thinking', () => {
    const onStop = vi.fn()
    const onRunCommand = vi.fn()
    render(<Composer thinking={true} onSend={() => {}} onStop={onStop} commands={SLASH_COMMANDS} onRunCommand={onRunCommand} />)
    const ta = screen.getByPlaceholderText('插入消息到当前回合…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: '/comp' } })
    fireEvent.keyDown(ta, { key: 'Escape' })
    expect(onStop).not.toHaveBeenCalled() // menu-close took precedence
    expect(screen.queryByText('/compact')).not.toBeInTheDocument()
  })

  it('disables the send button when input is empty or whitespace', () => {
    render(<Composer thinking={false} onSend={() => {}} onStop={() => {}} />)
    const btn = screen.getByLabelText('发送消息')
    expect(btn).toBeDisabled()
    const ta = screen.getByPlaceholderText('给 zuse 发消息…') as HTMLTextAreaElement
    fireEvent.change(ta, { target: { value: 'hi' } })
    expect(btn).not.toBeDisabled()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: FAIL（"Escape stops the turn while thinking" —— onKeyDown 菜单关分支尚无 Escape→onStop）

- [ ] **Step 3: 写实现** —— onKeyDown "菜单关"分支补 Escape

在"菜单关"分支的历史处理之后（Task 4 追加的块之后）加：
```tsx
            // 4) Menu closed: Escape stops the turn if one is running.
            if (e.key === 'Escape' && thinking) { e.preventDefault(); onStop(); return }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm -F @zuse/web exec vitest run src/components/Composer.test.tsx`
Expected: PASS（新增 4 条 + 既有全绿）

- [ ] **Step 5: 提交**

```bash
git add packages/web/src/components/Composer.tsx packages/web/src/components/Composer.test.tsx
git commit -m "feat(web): I4 Esc stops the turn; disabled send state"
```

---

## Task 6: `/work` 落点 —— DirPicker 可命令式打开

**Files:**
- Modify: `packages/web/src/components/DirPicker.tsx`
- Modify: `packages/web/src/components/Header.tsx`

> DirPicker 的 open 状态是内部的；给它一个 `useImperativeHandle` 暴露 `open()`，Header 透传一个 ref，供 Shell 的 `/work` 命令调用。此改动无独立单测（纯接线，覆盖在 Task 8 的手测清单）；靠 typecheck 守护。

- [ ] **Step 1: 改 DirPicker 为 forwardRef + 暴露 open()**

`DirPicker.tsx` 顶部 import 增补：
```tsx
import { forwardRef, useImperativeHandle, useRef, useState } from 'react'
```
（`useRef`/`useState` 已在原文件，合并即可，不要重复 import。）

导出句柄类型 + 用 forwardRef 包裹组件签名：
```tsx
export interface DirPickerHandle { open: () => void }

export const DirPicker = forwardRef<DirPickerHandle, { cwd: string; onChange: (cwd: string) => void }>(
  function DirPicker({ cwd, onChange }, ref) {
```
在 `openPicker` 定义之后加：
```tsx
    useImperativeHandle(ref, () => ({ open: openPicker }), [cwd])
```
文件末尾把原来的 `}` 结束改为 `})`（闭合 forwardRef 调用）。

- [ ] **Step 2: Header 透传 ref**

`Header.tsx` 顶部 import：
```tsx
import type { DirPickerHandle } from './DirPicker.js'
```
Header 的 props 签名加一个可选 `dirPickerRef`：
```tsx
export function Header({ state, onMenu, onOpenManage, onChangeCwd, dirPickerRef }: { state: AppState; onMenu: () => void; onOpenManage: () => void; onChangeCwd: (cwd: string) => void; dirPickerRef?: React.Ref<DirPickerHandle> }) {
```
把 `<DirPicker cwd=… onChange=… />` 改为：
```tsx
        <DirPicker ref={dirPickerRef} cwd={state.cwd ?? ''} onChange={onChangeCwd} />
```

- [ ] **Step 3: typecheck**

Run: `pnpm -F @zuse/web typecheck`
Expected: 通过（无类型错误）

- [ ] **Step 4: 提交**

```bash
git add packages/web/src/components/DirPicker.tsx packages/web/src/components/Header.tsx
git commit -m "feat(web): I4 DirPicker imperative open() for the /work command"
```

---

## Task 7: Shell 接线 —— 历史、命令 ctx、传参、删旧表

**Files:**
- Modify: `packages/web/src/components/Shell.tsx`

> 无独立单测（Composer 与 commands 的逻辑已各自被测）；靠 typecheck + Task 8 手测守护。

- [ ] **Step 1: 改 imports 与命令表**

删除旧的 `SLASH_COMMANDS` 常量（第 37–41 行的 `Record<string, ClientMessage>` 版本）。
imports 增补：
```tsx
import { useMemo } from 'react' // 合并进已有的 react import
import { SLASH_COMMANDS, type SlashCommand, type CommandContext } from './commands.js'
import type { DirPickerHandle } from './DirPicker.js'
```

同时在模块顶层（imports 之后、`Shell` 之前）加一个稳定的空历史常量，避免 `?? []` 每帧换新引用触发 Composer 的重置副作用：
```tsx
const EMPTY_HISTORY: string[] = []
```

- [ ] **Step 2: Shell 内加历史、dirPicker ref、ctx、run 回调**

在 `Shell()` 组件体内（`const [shareSel…]` 之后）加：
```tsx
  const historyRef = useRef<Map<string, string[]>>(new Map())
  const dirPickerRef = useRef<DirPickerHandle>(null)
  const focusHistorySearch = useCallback(() => {
    setMenuOpen(true)
    // The search box lives in the (now-open) Sidebar; focus it once it's on-screen.
    requestAnimationFrame(() => document.querySelector<HTMLInputElement>('.session-search')?.focus())
  }, [])
  const commandCtx: CommandContext = useMemo(() => ({
    send,
    newSession: () => { setShareSel(null); void newSession(state.cwd || undefined); setMenuOpen(false) },
    openPanel: (panel) => { setActivePanel(panel); setDrawerOpen(true) },
    focusHistorySearch,
    showHelp: () => { setActivePanel('memory'); setDrawerOpen(false); alert(SLASH_COMMANDS.map((c) => `${c.name} — ${c.desc}`).join('\n')) },
    openDirPicker: () => dirPickerRef.current?.open(),
  }), [send, newSession, state.cwd, focusHistorySearch])
  const onRunCommand = useCallback((cmd: SlashCommand) => cmd.run(commandCtx), [commandCtx])
  const currentHistory = historyRef.current.get(currentSessionId ?? '') ?? EMPTY_HISTORY
```

> `showHelp` 先用 `alert` 列命令（零 UI 成本、可用）；日后要更美观再换 notice/浮层，不改命令模型。`state.cwd` 变化时 ctx 重算即可，无需把 history 放进依赖。

- [ ] **Step 3: onSend 入历史**

把 `onSend` 改为（删掉旧的 `SLASH_COMMANDS[text.trim()]` 查表逻辑——slash 命令现在走命令菜单的 `onRunCommand`，不再从 onSend 里拦截）：
```tsx
  const onSend = (text: string) => {
    // Record into this session's in-memory history (slash commands never reach here — the menu runs
    // them via onRunCommand — so history stays clean).
    const key = currentSessionId ?? ''
    const arr = historyRef.current.get(key) ?? []
    historyRef.current.set(key, [...arr, text])
    if (state.thinking) {
      dispatch({ kind: 'steer-queued', id: nextId('ps'), text })
      send({ type: 'steer', text })
      return
    }
    dispatch({ kind: 'user-send', id: nextId('u'), text })
    send({ type: 'send', text })
  }
```

> 注意：因为 `currentHistory` 从 `historyRef` 读且 ref 变更不触发重渲染，追加后 Composer 拿到的 `history` prop 在下一次自然重渲染（send 引发的状态更新）时刷新——足够（历史只在用户按 ↑ 时用到，那时早已重渲染过）。

- [ ] **Step 4: 传参给 Header 与 Composer**

`<Header … />` 加 `dirPickerRef={dirPickerRef}`。
`<Composer thinking={state.thinking} onSend={onSend} onStop={…} />` 改为：
```tsx
          <Composer
            thinking={state.thinking}
            onSend={onSend}
            onStop={() => send({ type: 'interrupt' })}
            history={currentHistory}
            commands={SLASH_COMMANDS}
            onRunCommand={onRunCommand}
          />
```

- [ ] **Step 5: typecheck**

Run: `pnpm -F @zuse/web typecheck`
Expected: 通过

- [ ] **Step 6: 跑全 web 测试**

Run: `pnpm -F @zuse/web test`
Expected: 全绿（含 Composer/commands 新测 + 既有）

- [ ] **Step 7: 提交**

```bash
git add packages/web/src/components/Shell.tsx
git commit -m "feat(web): I4 wire history + command context into Shell"
```

---

## Task 8: 样式 —— 命令菜单浮层 + 按钮图标态

**Files:**
- Modify: `packages/web/src/styles.css`

- [ ] **Step 1: 加命令菜单与按钮样式**

在 `.composer-wrap`（第 249 行附近）之后追加：
```css
/* Slash-command autocomplete menu, pinned above the composer (same 7% inset as chat content). */
.slash-menu { list-style: none; margin: 0 7% 8px; padding: 6px; background: var(--surface); border: 1px solid var(--line); border-radius: 14px; box-shadow: 0 4px 18px var(--shadow); max-height: 260px; overflow-y: auto; }
.slash-item { display: flex; align-items: baseline; gap: 10px; padding: 7px 10px; border-radius: 9px; cursor: pointer; }
.slash-item.active, .slash-item:hover { background: var(--surface-2); }
.slash-name { font-weight: 600; font-size: 14px; }
.slash-desc { color: var(--muted); font-size: 12px; }
/* Send button: accent-filled, dims + no-cursor when disabled (empty input). */
.send-btn:disabled { opacity: 0.4; cursor: default; }
.send-btn svg, .stop-btn svg { display: block; }
.stop-btn { width: 36px; height: 36px; border-radius: 50%; padding: 0; display: flex; align-items: center; justify-content: center; flex: none; }
```

> `--surface` `--surface-2` `--line` `--muted` `--shadow` `--accent*` 均为 styles.css 既有的主题变量（见文件顶部 `:root`）；沿用即可，明暗主题自动适配。

- [ ] **Step 2: 重建 + 重启 daemon，手动验证**

Run: `pnpm -F @zuse/web build`
然后重启 daemon（用 `/restart` skill 或既有流程），浏览器 127.0.0.1:4180 验证：
1. 输入 `/` → 弹出全部命令；输入 `/co` → 只剩 `/compact`。
2. `↑/↓` 移动高亮，`Enter`/`Tab` 执行，`Esc` 关菜单。
3. `/skills` `/memory` 等打开对应面板；`/clear` 新建会话；`/work` 打开目录选择器；`/history` 打开侧栏并聚焦搜索框；`/help` 弹命令清单。
4. 空历史下 `↑` 无反应；发几条消息后 `↑/↓` 翻历史；多行时光标不在边界时方向键正常移动。
5. thinking 时按 `Esc` 停止；停止按钮为方块图标；发送按钮为回车箭头图标、空输入置灰。
6. 中文输入法组字时回车不误发。

- [ ] **Step 3: 提交**

```bash
git add packages/web/src/styles.css
git commit -m "style(web): I4 slash-menu popover + icon-button states"
```

---

## Self-Review 记录

- **Spec 覆盖**：§2 命令模型→Task 1/7；§3.1 历史→Task 4；§3.2 `/` 菜单→Task 2/3；§3.3 图标按钮→Task 2(实现)/5(禁用测)/8(CSS)；§3.4 键盘优先级→Task 3/4/5 累积的单一 onKeyDown；§6 测试项 1–9→分散在 Task 2–5 的用例；命令集 §2 表全部落在 Task 1 的 `SLASH_COMMANDS`。
- **命令集落点**：`/work`→Task 6 imperative open；`/history`→Task 7 querySelector focus；面板类→Task 7 openPanel；`/help`→alert（Task 7，标注可后续美化）。
- **类型一致性**：`SlashCommand`/`CommandContext`（Task 1）→ Composer props（Task 2）与 Shell ctx（Task 7）一致；`DirPickerHandle`（Task 6）→ Shell ref（Task 7）一致；`filterCommands` 签名 `(input, commands)` 全程一致。
- **既有测试**：Task 2 Step 5 已处理"停止"文本按钮改图标导致的既有断言失效。
```
