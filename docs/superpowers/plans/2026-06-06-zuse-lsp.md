# LSP 代码智能工具 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 zuse 加一个只读 `Lsp` 工具,支持定义跳转 / 找引用 / 悬停类型三种查询,底层走标准 LSP(JSON-RPC over stdio),按 cwd 懒启动并复用语言服务器进程。

**Architecture:** 数据驱动的 `LANGUAGE_SERVERS` 配置表(加语言=加一条)+ 会话级进程池 `LspManager`(`Map<langId, LspClient>`)+ 单进程封装 `LspClient`(vscode-jsonrpc 连接 + 五阶段生命周期)+ 纯函数 `symbol.ts`(符号名+行号→Position)与结果格式化。工具 `readOnly:true` 不进权限闸。

**Tech Stack:** TypeScript(ESM no-build,原始 ./src),`vscode-jsonrpc`(传输框架)+ `vscode-languageserver-protocol`(LSP 类型),vitest,pnpm 9 / Node 22。

**Spec:** [docs/superpowers/specs/2026-06-06-zuse-lsp-design.md](../specs/2026-06-06-zuse-lsp-design.md) —— 实现时以 spec 为准,本计划是它的任务分解。

**约束(全程遵守):**
- 所有代码注释用中文。
- **不要 git commit** —— 用户要求评审前不提交。每个 Task 末尾的「Commit」步骤改为:`git add` 暂存改动后**停在此处**,不执行 commit(留待用户评审)。
- 测试命令:仓库根 `npm test`(vitest run);单文件 `npx vitest run packages/tools/src/<file>.test.ts`。
- 类型检查:`pnpm -r typecheck`。Lint:`npx eslint <files>`。
- import 用 `.js` 扩展(ESM 约定,即便源文件是 .ts)。

---

## Task 1: 加依赖并验证 ESM 可导入性(spike)

**Files:**
- Modify: `packages/tools/package.json`(dependencies + devDependencies)
- Create(临时探针,验证后删除): `packages/tools/src/lsp/__spike.ts`

- [ ] **Step 1: 安装运行时依赖**

在仓库根运行:
```bash
pnpm -F @zuse/tools add vscode-jsonrpc@^9.0.0 vscode-languageserver-protocol@^3.18.0
pnpm -F @zuse/tools add -D typescript-language-server@^4.3.3
```
(`typescript` 已在根 devDependencies,集成测试可用。)

- [ ] **Step 2: 写探针验证命名导入在 Node ESM 下可用**

创建 `packages/tools/src/lsp/__spike.ts`:
```ts
// 验证两个 CJS 包在 zuse 的 ESM no-build 下能否命名导入。
import { createMessageConnection, StreamMessageReader, StreamMessageWriter } from 'vscode-jsonrpc/node'
import { InitializeRequest, DefinitionRequest, ReferencesRequest, HoverRequest, DidOpenTextDocumentNotification } from 'vscode-languageserver-protocol'

console.log(typeof createMessageConnection, typeof StreamMessageReader, typeof StreamMessageWriter)
console.log(InitializeRequest.method, DefinitionRequest.method, ReferencesRequest.method, HoverRequest.method, DidOpenTextDocumentNotification.method)
```

- [ ] **Step 3: 运行探针**

Run: `cd /e/ai-study/zuse && npx tsx packages/tools/src/lsp/__spike.ts`
Expected: 打印 `function function function` 和五个方法名(`initialize textDocument/definition textDocument/references textDocument/hover textDocument/didOpen`),无 import 报错。

**若命名导入报错**(`SyntaxError: Named export ... not found`):改用默认导入兜底,并在后续所有任务沿用该形式:
```ts
import jsonrpc from 'vscode-jsonrpc/node'
const { createMessageConnection, StreamMessageReader, StreamMessageWriter } = jsonrpc
import lsp from 'vscode-languageserver-protocol'
const { InitializeRequest /* ... */ } = lsp
```
记录哪种形式可用 —— 这是后续 client.ts 的导入基准。

- [ ] **Step 4: 删除探针**

```bash
rm packages/tools/src/lsp/__spike.ts
```

- [ ] **Step 5: 暂存(不提交)**

```bash
git add packages/tools/package.json ../../pnpm-lock.yaml
# 停在此处,不 commit
```

---

## Task 2: 抽取 killTree / findOnPath 到 util.ts

**Files:**
- Modify: `packages/tools/src/util.ts`(新增并导出 `killTree`、`findOnPath`)
- Modify: `packages/tools/src/bash.ts`(改为 import,删本地定义)
- Test: `packages/tools/src/util.test.ts`(新增)

- [ ] **Step 1: 写 util 的失败测试**

向 `packages/tools/src/util.test.ts` 追加(若文件不存在则创建,顶部 `import { describe, it, expect } from 'vitest'`):
```ts
import { findOnPath, killTree } from './util.js'

describe('findOnPath', () => {
  it('finds node on PATH', () => {
    // node 一定在 PATH 上(测试用 node 跑)
    const exe = process.platform === 'win32' ? 'node.exe' : 'node'
    expect(findOnPath(exe)).toBeTruthy()
  })
  it('returns undefined for a missing executable', () => {
    expect(findOnPath('definitely-not-a-real-exe-xyz.zzz')).toBeUndefined()
  })
})

describe('killTree', () => {
  it('is a no-op for undefined pid', () => {
    expect(() => killTree(undefined)).not.toThrow()
  })
})
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `npx vitest run packages/tools/src/util.test.ts`
Expected: FAIL —— `findOnPath`/`killTree` is not exported from './util.js'。

- [ ] **Step 3: 把两个函数移到 util.ts**

在 `packages/tools/src/util.ts` 顶部加 import 并追加函数(原样从 bash.ts 搬,保留中文注释):
```ts
import { spawn } from 'node:child_process'
import { existsSync } from 'node:fs'
import path from 'node:path'

/** 在 PATH 列出的目录里找一个可执行文件，返回首个命中的绝对路径。 */
export function findOnPath(exe: string): string | undefined {
  for (const dir of (process.env.PATH ?? '').split(path.delimiter)) {
    if (!dir) continue
    const full = path.join(dir, exe)
    if (existsSync(full)) return full
  }
  return undefined
}

/**
 * 杀掉整棵进程树。child 是被 spawn 的进程，真正干活的命令可能是它的子进程。
 * 只 kill 父进程会留下占着管道的孙进程。Windows 用 taskkill /T 杀树，POSIX 杀进程组。
 */
export function killTree(pid: number | undefined): void {
  if (pid === undefined) return
  if (process.platform === 'win32') {
    spawn('taskkill', ['/pid', String(pid), '/T', '/F'])
  } else {
    try {
      process.kill(-pid, 'SIGTERM') // 负 pid = 整个进程组
    } catch {
      process.kill(pid, 'SIGTERM')
    }
  }
}
```

- [ ] **Step 4: 改 bash.ts 用 import**

在 `packages/tools/src/bash.ts`:删除本地的 `findOnPath`(14-22 行)与 `killTree`(119-130 行)定义;在顶部 import 区加入 `import { findOnPath, killTree } from './util.js'`。`resolveShell`/`gitBashUnder` 保留不动(它们用到 `findOnPath`,现从 util 导入)。注意 bash.ts 顶部若因此不再直接用 `spawn`/`existsSync`/`path` 之外的符号,保留仍被使用的 import(`spawn` 仍用于 spawn 命令、`existsSync`/`path` 仍用于 resolveShell)。

- [ ] **Step 5: 运行 util + bash 测试**

Run: `npx vitest run packages/tools/src/util.test.ts packages/tools/src/bash.test.ts`
Expected: 全部 PASS。

- [ ] **Step 6: 类型检查 + lint**

Run: `pnpm -r typecheck && npx eslint packages/tools/src/util.ts packages/tools/src/bash.ts`
Expected: 无错误。

- [ ] **Step 7: 暂存(不提交)** `git add packages/tools/src/util.ts packages/tools/src/util.test.ts packages/tools/src/bash.ts`

---

## Task 3: servers.ts —— 语言配置表 + lookupLanguage

**Files:**
- Create: `packages/tools/src/lsp/servers.ts`
- Test: `packages/tools/src/lsp/servers.test.ts`

- [ ] **Step 1: 写失败测试**

`packages/tools/src/lsp/servers.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { lookupLanguage, LANGUAGE_SERVERS } from './servers.js'

describe('lookupLanguage', () => {
  it('maps .ts and .tsx to typescript', () => {
    expect(lookupLanguage('/x/a.ts')?.id).toBe('typescript')
    expect(lookupLanguage('/x/a.tsx')?.id).toBe('typescript')
  })
  it('maps .py to python, .go to go, .rs to rust, .java to java, .vue to vue, .lua to lua, .sh to bash', () => {
    expect(lookupLanguage('a.py')?.id).toBe('python')
    expect(lookupLanguage('a.go')?.id).toBe('go')
    expect(lookupLanguage('a.rs')?.id).toBe('rust')
    expect(lookupLanguage('a.java')?.id).toBe('java')
    expect(lookupLanguage('a.vue')?.id).toBe('vue')
    expect(lookupLanguage('a.lua')?.id).toBe('lua')
    expect(lookupLanguage('a.sh')?.id).toBe('bash')
  })
  it('is case-insensitive on extension', () => {
    expect(lookupLanguage('A.TS')?.id).toBe('typescript')
  })
  it('returns null for unknown extension', () => {
    expect(lookupLanguage('a.docx')).toBeNull()
    expect(lookupLanguage('noext')).toBeNull()
  })
  it('every config has command, extensions, ready, installHint', () => {
    for (const c of LANGUAGE_SERVERS) {
      expect(c.command).toBeTruthy()
      expect(c.extensions.length).toBeGreaterThan(0)
      expect(['immediate', 'awaitProgress', 'awaitNotification']).toContain(c.ready)
      expect(c.installHint).toBeTruthy()
    }
  })
  it('java config carries readyNotification and dataDirArg', () => {
    const java = LANGUAGE_SERVERS.find((c) => c.id === 'java')!
    expect(java.ready).toBe('awaitNotification')
    expect(java.readyNotification).toBe('language/status')
    expect(java.dataDirArg?.('/tmp/x')).toEqual(['-data', '/tmp/x'])
  })
})
```

- [ ] **Step 2: 运行,确认失败** Run: `npx vitest run packages/tools/src/lsp/servers.test.ts` — Expected: FAIL(模块不存在)。

- [ ] **Step 3: 实现 servers.ts**

```ts
/** 服务器就绪策略 —— 何时认为「可以开始查了」。 */
export type ReadyStrategy = 'immediate' | 'awaitProgress' | 'awaitNotification'

/** 单门语言的服务器配置。加语言 = 加一条。 */
export interface LanguageServerConfig {
  id: string
  extensions: string[]
  command: string
  args: string[]
  initializationOptions?: unknown
  ready: ReadyStrategy
  readyNotification?: string
  dataDirArg?: (tmpDir: string) => string[]
  installHint: string
}

/** v1 预置 8 门语言。加一门 = 往这个数组加一条,无需改其它代码。 */
export const LANGUAGE_SERVERS: LanguageServerConfig[] = [
  {
    id: 'typescript',
    extensions: ['.ts', '.tsx', '.js', '.jsx', '.mjs', '.cjs'],
    command: 'typescript-language-server',
    args: ['--stdio'],
    ready: 'immediate',
    installHint: 'npm i -g typescript-language-server typescript',
  },
  {
    id: 'python',
    extensions: ['.py', '.pyi'],
    command: 'pyright-langserver',
    args: ['--stdio'],
    ready: 'immediate',
    installHint: 'npm i -g pyright',
  },
  {
    id: 'vue',
    extensions: ['.vue'],
    command: 'vue-language-server',
    args: ['--stdio'],
    ready: 'immediate',
    installHint: 'npm i -g @vue/language-server（Volar 需 initializationOptions.typescript.tsdk 指向本机 typescript lib 目录）',
  },
  {
    id: 'java',
    extensions: ['.java'],
    command: 'jdtls',
    args: [],
    ready: 'awaitNotification',
    readyNotification: 'language/status',
    dataDirArg: (tmpDir: string): string[] => ['-data', tmpDir],
    installHint: '安装 Eclipse JDT LS（提供 jdtls 启动脚本）；本机需有 JDK',
  },
  {
    id: 'rust',
    extensions: ['.rs'],
    command: 'rust-analyzer',
    args: [],
    ready: 'awaitProgress',
    installHint: 'rustup component add rust-analyzer',
  },
  {
    id: 'go',
    extensions: ['.go'],
    command: 'gopls',
    args: [],
    ready: 'awaitProgress',
    installHint: 'go install golang.org/x/tools/gopls@latest',
  },
  {
    id: 'lua',
    extensions: ['.lua'],
    command: 'lua-language-server',
    args: [],
    ready: 'immediate',
    installHint: '安装 lua-language-server（scoop/brew/手动）',
  },
  {
    id: 'bash',
    extensions: ['.sh', '.bash'],
    command: 'bash-language-server',
    args: ['start'],
    ready: 'immediate',
    installHint: 'npm i -g bash-language-server',
  },
]

/** 按文件扩展名(小写)查语言配置;未命中返回 null。 */
export function lookupLanguage(filePath: string): LanguageServerConfig | null {
  const lower = filePath.toLowerCase()
  const dot = lower.lastIndexOf('.')
  if (dot < 0) return null
  const ext = lower.slice(dot)
  return LANGUAGE_SERVERS.find((c) => c.extensions.includes(ext)) ?? null
}
```

- [ ] **Step 4: 运行,确认通过** Run: `npx vitest run packages/tools/src/lsp/servers.test.ts` — Expected: PASS。
- [ ] **Step 5: typecheck + lint** Run: `pnpm -r typecheck && npx eslint packages/tools/src/lsp/servers.ts`
- [ ] **Step 6: 暂存** `git add packages/tools/src/lsp/servers.ts packages/tools/src/lsp/servers.test.ts`

---

## Task 4: symbol.ts —— locateSymbol

**Files:**
- Create: `packages/tools/src/lsp/symbol.ts`
- Test: `packages/tools/src/lsp/symbol.test.ts`

- [ ] **Step 1: 写失败测试**

`packages/tools/src/lsp/symbol.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { locateSymbol } from './symbol.js'

const TEXT = [
  'const foo = 1',          // line 1 (1-based)
  'function bar() {',       // line 2
  '  return foobar + foo',  // line 3: foobar(word) 与 foo
  '}',                      // line 4
].join('\n')

describe('locateSymbol', () => {
  it('finds first occurrence across file when no line given', () => {
    const r = locateSymbol(TEXT, 'foo')!
    expect(r.matchedLine).toBe(1)
    expect(r.position).toEqual({ line: 0, character: 6 }) // 0-based: 'const ' = 6
  })
  it('respects the given 1-based line', () => {
    const r = locateSymbol(TEXT, 'foo', 3)!
    expect(r.matchedLine).toBe(3)
    // 第3行 '  return foobar + foo' 中,词边界 foo 在 '+ ' 之后,index 17
    expect(r.position).toEqual({ line: 2, character: 17 })
  })
  it('uses word boundaries (foo does not match inside foobar)', () => {
    // 第3行同时有 foobar 和 foo;查 foo 应跳过 foobar 命中末尾的 foo
    const r = locateSymbol(TEXT, 'foo', 3)!
    expect(r.position.character).toBe(17)
  })
  it('locates foobar itself', () => {
    const r = locateSymbol(TEXT, 'foobar', 3)!
    expect(r.position).toEqual({ line: 2, character: 9 })
  })
  it('returns null when symbol absent', () => {
    expect(locateSymbol(TEXT, 'nope')).toBeNull()
    expect(locateSymbol(TEXT, 'foo', 2)).toBeNull() // 第2行没有 foo
  })
  it('escapes regex metacharacters in symbol', () => {
    expect(() => locateSymbol('a.b.c', 'a.b')).not.toThrow()
    // 'a.b' 作为字面量,词边界下 . 不是 \w,匹配从 index 0 开始
    const r = locateSymbol('xx a.b yy', 'a.b')
    expect(r).not.toBeNull()
  })
})
```
> 注:实现完成后若个别 character 列与预期差 1,以实现为准回填测试期望值(列偏移是确定的,按实际字符串数)。词边界与「跳过 foobar」是硬断言,不可改。

- [ ] **Step 2: 运行,确认失败** Run: `npx vitest run packages/tools/src/lsp/symbol.test.ts` — Expected: FAIL。

- [ ] **Step 3: 实现 symbol.ts**

```ts
import type { Position } from 'vscode-languageserver-protocol'

export interface SymbolLocation {
  /** 0-based LSP 位置(character 为 UTF-16 码元偏移)。 */
  position: Position
  /** 实际命中的 1-based 行号,回喂给用户看。 */
  matchedLine: number
}

/** 正则元字符转义,让 symbol 当字面量匹配。 */
function escapeRegExp(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}

/**
 * 在 text 里按词边界定位 symbol。
 * 给了 line(1-based)就只在该行找首次出现;没给就全文逐行找首个命中行的首次出现。
 * 找不到返回 null。
 */
export function locateSymbol(text: string, symbol: string, line?: number): SymbolLocation | null {
  const lines = text.split('\n')
  const re = new RegExp(`\\b${escapeRegExp(symbol)}\\b`)
  const search = (idx: number): SymbolLocation | null => {
    const m = re.exec(lines[idx])
    return m ? { position: { line: idx, character: m.index }, matchedLine: idx + 1 } : null
  }
  if (line !== undefined) {
    const idx = line - 1
    if (idx < 0 || idx >= lines.length) return null
    return search(idx)
  }
  for (let i = 0; i < lines.length; i++) {
    const r = search(i)
    if (r) return r
  }
  return null
}
```

- [ ] **Step 4: 运行,确认通过(必要时按实现回填 character 期望)** Run: `npx vitest run packages/tools/src/lsp/symbol.test.ts` — Expected: PASS。
- [ ] **Step 5: typecheck + lint** Run: `pnpm -r typecheck && npx eslint packages/tools/src/lsp/symbol.ts`
- [ ] **Step 6: 暂存** `git add packages/tools/src/lsp/symbol.ts packages/tools/src/lsp/symbol.test.ts`

---

## Task 5: 结果格式化(format.ts,纯函数)

**Files:**
- Create: `packages/tools/src/lsp/format.ts`
- Test: `packages/tools/src/lsp/format.test.ts`

格式化只依赖 LSP 的 `Location`/`Hover` 类型 + 一个「读某文件某行源码」的回调(注入以便纯测,不打真实 fs)。

- [ ] **Step 1: 写失败测试**

`packages/tools/src/lsp/format.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { uriToRelPath, formatDefinition, formatReferences, formatHover } from './format.js'

const CWD = process.platform === 'win32' ? 'C:\\proj' : '/proj'
function uri(rel: string): string {
  // 构造一个 file:// uri,相对 CWD
  const abs = (process.platform === 'win32' ? 'C:\\proj\\' : '/proj/') + rel
  return process.platform === 'win32'
    ? 'file:///C:/proj/' + rel
    : 'file://' + abs
}
const lineText = (): string => '  export function fooBar(x) {'

describe('uriToRelPath', () => {
  it('converts a file uri to a path relative to cwd', () => {
    expect(uriToRelPath(uri('src/a.ts'), CWD).replace(/\\/g, '/')).toBe('src/a.ts')
  })
})

describe('formatDefinition', () => {
  it('renders file:line:col with the source line', () => {
    const loc = { uri: uri('src/a.ts'), range: { start: { line: 41, character: 16 }, end: { line: 41, character: 22 } } }
    const out = formatDefinition([loc], CWD, () => lineText())
    expect(out).toContain('src/a.ts:42:17')
    expect(out).toContain('export function fooBar')
  })
  it('reports not found on empty', () => {
    expect(formatDefinition([], CWD, () => '', 'foo')).toMatch(/not found/i)
  })
})

describe('formatReferences', () => {
  it('groups by file and caps at the limit', () => {
    const locs = Array.from({ length: 150 }, (_, i) => ({
      uri: uri('src/a.ts'),
      range: { start: { line: i, character: 0 }, end: { line: i, character: 3 } },
    }))
    const out = formatReferences(locs, CWD, () => 'someLine', 100)
    expect(out).toMatch(/and 50 more/i)
  })
})

describe('formatHover', () => {
  it('extracts MarkupContent value', () => {
    expect(formatHover({ contents: { kind: 'markdown', value: 'type: number' } } as never, 'x')).toContain('type: number')
  })
  it('extracts plain string contents', () => {
    expect(formatHover({ contents: 'plain doc' } as never, 'x')).toContain('plain doc')
  })
  it('reports no info on null', () => {
    expect(formatHover(null, 'foo')).toMatch(/no hover/i)
  })
})
```
> 注:`uri` 的构造与 `uriToRelPath` 需在 win/posix 都成立;实现用 `vscode-uri` 不可用(未引入),改用 Node 的 `url.fileURLToPath`/`pathToFileURL` 做 uri↔路径转换,再 `path.relative(cwd, abs)`。若个别行列期望值与实现差 1,以实现回填(definition 的 1-based 转换是硬规则:0-based 41→42、16→17)。

- [ ] **Step 2: 运行,确认失败** Run: `npx vitest run packages/tools/src/lsp/format.test.ts` — Expected: FAIL。

- [ ] **Step 3: 实现 format.ts**

```ts
import { fileURLToPath } from 'node:url'
import path from 'node:path'
import type { Location, Hover, MarkupContent } from 'vscode-languageserver-protocol'

/** 读某绝对路径某 0-based 行的源码(注入以便测试)。 */
export type LineReader = (absPath: string, line0: number) => string

const MAX_HOVER = 4_000

/** file:// uri → 相对 cwd 的路径(展示用)。 */
export function uriToRelPath(uri: string, cwd: string): string {
  const abs = fileURLToPath(uri)
  const rel = path.relative(cwd, abs)
  return rel || abs
}

/** definition:每条 路径:行:列 + 目标行源码。空 → not found。 */
export function formatDefinition(locs: Location[], cwd: string, readLine: LineReader, symbol?: string): string {
  if (locs.length === 0) return `Definition not found${symbol ? ` for: ${symbol}` : ''}`
  return locs
    .map((l) => {
      const rel = uriToRelPath(l.uri, cwd)
      const ln = l.range.start.line
      const col = l.range.start.character
      const src = readLine(fileURLToPath(l.uri), ln).trim()
      return `${rel}:${ln + 1}:${col + 1}\n  ${src}`
    })
    .join('\n\n')
}

/** references:按文件分组,每条 行:列 + 行文本;超过 limit 截断。 */
export function formatReferences(locs: Location[], cwd: string, readLine: LineReader, limit: number, symbol?: string): string {
  if (locs.length === 0) return `No references found${symbol ? ` for: ${symbol}` : ''}`
  const shown = locs.slice(0, limit)
  const byFile = new Map<string, Location[]>()
  for (const l of shown) {
    const rel = uriToRelPath(l.uri, cwd)
    const arr = byFile.get(rel) ?? []
    arr.push(l)
    byFile.set(rel, arr)
  }
  const blocks: string[] = []
  for (const [rel, group] of byFile) {
    const lines = group.map((l) => {
      const src = readLine(fileURLToPath(l.uri), l.range.start.line).trim()
      return `  ${l.range.start.line + 1}:${l.range.start.character + 1}  ${src}`
    })
    blocks.push(`${rel}\n${lines.join('\n')}`)
  }
  let out = `Found ${locs.length} reference(s):\n\n${blocks.join('\n\n')}`
  if (locs.length > limit) out += `\n\n… and ${locs.length - limit} more references`
  return out
}

/** hover:抽取 contents 文本(MarkupContent / MarkedString / 数组 / string)。null → no info。 */
export function formatHover(hover: Hover | null, symbol?: string): string {
  if (!hover || hover.contents == null) return `No hover info${symbol ? ` for: ${symbol}` : ''}`
  const c = hover.contents
  let text: string
  if (typeof c === 'string') text = c
  else if (Array.isArray(c)) text = c.map((p) => (typeof p === 'string' ? p : p.value)).join('\n')
  else if (typeof (c as MarkupContent).value === 'string') text = (c as MarkupContent).value
  else text = String((c as { value?: unknown }).value ?? '')
  text = text.trim()
  if (text.length > MAX_HOVER) text = text.slice(0, MAX_HOVER) + '\n…[truncated]'
  return text || `No hover info${symbol ? ` for: ${symbol}` : ''}`
}
```

- [ ] **Step 4: 运行,确认通过** Run: `npx vitest run packages/tools/src/lsp/format.test.ts` — Expected: PASS。
- [ ] **Step 5: typecheck + lint** Run: `pnpm -r typecheck && npx eslint packages/tools/src/lsp/format.ts`
- [ ] **Step 6: 暂存** `git add packages/tools/src/lsp/format.ts packages/tools/src/lsp/format.test.ts`

---

## Task 6: LspClient —— 单进程 + vscode-jsonrpc 连接 + 生命周期

**Files:**
- Create: `packages/tools/src/lsp/client.ts`

无独立单测(spawn 真进程,留给 Task 9 的 TS 集成测)。本任务以「typecheck + 被 Task 9 集成测覆盖」为验收。

- [ ] **Step 1: 实现 client.ts**

> 用 Task 1 确认的导入形式。下面按命名导入写;若 Task 1 判定需默认导入,按那个改。`rootUri`/文件 uri 用 `pathToFileURL`。请求超时与就绪超时见常量。

```ts
import { spawn, type ChildProcessWithoutNullStreams } from 'node:child_process'
import { pathToFileURL } from 'node:url'
import {
  createMessageConnection,
  StreamMessageReader,
  StreamMessageWriter,
  type MessageConnection,
} from 'vscode-jsonrpc/node'
import {
  InitializeRequest,
  InitializedNotification,
  DidOpenTextDocumentNotification,
  DefinitionRequest,
  ReferencesRequest,
  HoverRequest,
  ShutdownRequest,
  ExitNotification,
  type Position,
  type Location,
  type LocationLink,
  type Hover,
  type InitializeParams,
} from 'vscode-languageserver-protocol'
import { killTree } from '../util.js'
import type { LanguageServerConfig } from './servers.js'

const READY_TIMEOUT = 30_000
const REQUEST_TIMEOUT = 20_000

/** 类型化 LSP 错误;installHint 用于「没装」场景回喂安装命令。 */
export class LspError extends Error {
  constructor(message: string, readonly installHint?: string) {
    super(message)
  }
}

/** 把 definition 的多种返回(Location | Location[] | LocationLink[])归一为 Location[]。 */
function toLocations(result: Location | Location[] | LocationLink[] | null): Location[] {
  if (!result) return []
  const arr = Array.isArray(result) ? result : [result]
  return arr.map((r) =>
    'targetUri' in r ? { uri: r.targetUri, range: r.targetRange } : (r as Location),
  )
}

/** 给一个 Promise 加超时;到点 reject。 */
function withTimeout<T>(p: Promise<T>, ms: number, label: string): Promise<T> {
  return new Promise<T>((resolve, reject) => {
    const t = setTimeout(() => reject(new LspError(`${label} timed out after ${ms}ms`)), ms)
    p.then((v) => { clearTimeout(t); resolve(v) }, (e) => { clearTimeout(t); reject(e) })
  })
}

export class LspClient {
  private opened = new Set<string>()
  private constructor(
    private child: ChildProcessWithoutNullStreams,
    private conn: MessageConnection,
    private readonly config: LanguageServerConfig,
  ) {}

  /** 启动:spawn → 建连 → initialize 握手 → 就绪等待。失败抛 LspError。 */
  static async start(config: LanguageServerConfig, cwd: string, dataDir: string | undefined, signal: AbortSignal): Promise<LspClient> {
    const args = [...config.args, ...(config.dataDirArg && dataDir ? config.dataDirArg(dataDir) : [])]
    let child: ChildProcessWithoutNullStreams
    try {
      child = spawn(config.command, args, { cwd }) as ChildProcessWithoutNullStreams
    } catch (e) {
      throw new LspError(`Failed to spawn ${config.command}: ${(e as Error).message}`, config.installHint)
    }
    // spawn 的 ENOENT 是异步通过 'error' 事件来的,需监听后等握手。
    const spawnErr = new Promise<never>((_, reject) => {
      child.on('error', (e: NodeJS.ErrnoException) => {
        reject(new LspError(
          e.code === 'ENOENT'
            ? `Language server '${config.command}' not found. Install: ${config.installHint}`
            : `${config.command} failed: ${e.message}`,
          config.installHint,
        ))
      })
    })
    const conn = createMessageConnection(new StreamMessageReader(child.stdout), new StreamMessageWriter(child.stdin))
    const client = new LspClient(child, conn, config)
    const ready = client.handshakeAndReady(cwd, signal)
    await Promise.race([ready, spawnErr])
    return client
  }

  private async handshakeAndReady(cwd: string, signal: AbortSignal): Promise<void> {
    // 就绪通知/进度要在 initialize 之前挂好监听,避免竞态丢通知。
    let resolveReady!: () => void
    const readyByNotification = new Promise<void>((r) => { resolveReady = r })
    if (this.config.ready === 'awaitNotification' && this.config.readyNotification) {
      this.conn.onNotification(this.config.readyNotification, (p: unknown) => {
        // jdtls: { type: 'ServiceReady' }
        if (!p || (p as { type?: string }).type === 'ServiceReady') resolveReady()
      })
    }
    if (this.config.ready === 'awaitProgress') {
      this.conn.onProgress?.(/* WorkDoneProgress */ { method: '$/progress' } as never, '__never__', () => {})
      // 简化:监听 end 类进度。实现细节见下方说明,允许用 onNotification('$/progress') 兜底。
      this.conn.onNotification('$/progress', (p: { value?: { kind?: string } }) => {
        if (p?.value?.kind === 'end') resolveReady()
      })
    }
    this.conn.listen()
    const params: InitializeParams = {
      processId: process.pid,
      rootUri: pathToFileURL(cwd).toString(),
      capabilities: {},
      initializationOptions: this.config.initializationOptions,
      workspaceFolders: [{ uri: pathToFileURL(cwd).toString(), name: 'root' }],
    }
    await withTimeout(this.conn.sendRequest(InitializeRequest.type, params), READY_TIMEOUT, 'initialize')
    this.conn.sendNotification(InitializedNotification.type, {})
    if (this.config.ready === 'immediate') return
    // 慢启动:等通知/进度,或就绪超时后降级放行(不抛错)。
    await Promise.race([
      readyByNotification,
      new Promise<void>((r) => setTimeout(r, READY_TIMEOUT)),
    ])
    void signal // 启动期的 abort 由上层 race 处理;此处占位避免 lint 未用告警
  }

  /** 首次查某文件前发 didOpen(同一文件只 open 一次)。 */
  openDocument(absPath: string, text: string): void {
    const uri = pathToFileURL(absPath).toString()
    if (this.opened.has(uri)) return
    this.conn.sendNotification(DidOpenTextDocumentNotification.type, {
      textDocument: { uri, languageId: this.config.id, version: 1, text },
    })
    this.opened.add(uri)
  }

  async definition(absPath: string, position: Position): Promise<Location[]> {
    const uri = pathToFileURL(absPath).toString()
    const r = await withTimeout(
      this.conn.sendRequest(DefinitionRequest.type, { textDocument: { uri }, position }),
      REQUEST_TIMEOUT, 'definition',
    )
    return toLocations(r as Location | Location[] | LocationLink[] | null)
  }

  async references(absPath: string, position: Position): Promise<Location[]> {
    const uri = pathToFileURL(absPath).toString()
    const r = await withTimeout(
      this.conn.sendRequest(ReferencesRequest.type, { textDocument: { uri }, position, context: { includeDeclaration: true } }),
      REQUEST_TIMEOUT, 'references',
    )
    return (r as Location[] | null) ?? []
  }

  async hover(absPath: string, position: Position): Promise<Hover | null> {
    const uri = pathToFileURL(absPath).toString()
    const r = await withTimeout(
      this.conn.sendRequest(HoverRequest.type, { textDocument: { uri }, position }),
      REQUEST_TIMEOUT, 'hover',
    )
    return (r as Hover | null) ?? null
  }

  /** 优雅关闭:shutdown + exit,短宽限后 killTree 兜底。 */
  async dispose(): Promise<void> {
    try {
      await withTimeout(this.conn.sendRequest(ShutdownRequest.type, undefined), 2_000, 'shutdown')
      this.conn.sendNotification(ExitNotification.type)
    } catch {
      // 忽略,直接杀树
    }
    setTimeout(() => killTree(this.child.pid), 500)
    this.conn.dispose()
  }
}
```

> **实现注意(交给 implementer 在跑通集成测时校准):**
> - `awaitProgress` 的精确 API:vscode-jsonrpc 的 `onProgress` 需要 `ProgressType` token 才能用,这里实际更稳的是直接 `onNotification('$/progress', ...)` 监听 `value.kind === 'end'`。上面草稿里的 `onProgress?.(...)` 占位行应删除,只保留 `onNotification('$/progress')` 那条。跑 gopls/rust-analyzer 时确认能收到 end;收不到就靠 READY_TIMEOUT 降级。
> - `sendRequest(ShutdownRequest.type, undefined)` 的入参形态以 vscode-languageserver-protocol 的类型为准(shutdown 无参,可能需 `sendRequest(ShutdownRequest.type)`)。
> - 若 Task 1 判定默认导入,把两个 import 改成默认导入解构。
> - `child.stdout`/`child.stdin` 在 `spawn` 无 `stdio:'ignore'` 时非空;用 `as ChildProcessWithoutNullStreams` 收窄。

- [ ] **Step 2: typecheck** Run: `pnpm -r typecheck` — Expected: 无 client.ts 类型错误(删除 onProgress 占位行后)。
- [ ] **Step 3: lint** Run: `npx eslint packages/tools/src/lsp/client.ts`
- [ ] **Step 4: 暂存** `git add packages/tools/src/lsp/client.ts`

---

## Task 7: LspManager —— 会话级进程池

**Files:**
- Create: `packages/tools/src/lsp/manager.ts`
- Test: `packages/tools/src/lsp/manager.test.ts`(in-flight 去重用假 client 验证;真 spawn 留给 Task 9)

- [ ] **Step 1: 写失败测试(去重逻辑,注入假 starter)**

为可测,`LspManager` 接受一个可选的 `starter`(默认 `LspClient.start`),测试注入计数桩:
```ts
import { describe, it, expect, vi } from 'vitest'
import { LspManager } from './manager.js'
import type { LanguageServerConfig } from './servers.js'

const cfg = { id: 'fake', extensions: ['.fk'], command: 'x', args: [], ready: 'immediate', installHint: '' } as LanguageServerConfig

describe('LspManager', () => {
  it('dedupes concurrent starts of the same language (one spawn)', async () => {
    let starts = 0
    const fakeClient = { dispose: vi.fn(async () => {}) }
    const starter = vi.fn(async () => { starts++; await new Promise((r) => setTimeout(r, 20)); return fakeClient as never })
    const m = new LspManager(starter)
    m.setCwd(process.cwd())
    const [a, b] = await Promise.all([
      m.getClient(cfg, new AbortController().signal),
      m.getClient(cfg, new AbortController().signal),
    ])
    expect(a).toBe(b)
    expect(starts).toBe(1)
    await m.dispose()
    expect(fakeClient.dispose).toHaveBeenCalled()
  })
  it('reuses an already-started client', async () => {
    const fakeClient = { dispose: vi.fn(async () => {}) }
    const starter = vi.fn(async () => fakeClient as never)
    const m = new LspManager(starter)
    m.setCwd(process.cwd())
    await m.getClient(cfg, new AbortController().signal)
    await m.getClient(cfg, new AbortController().signal)
    expect(starter).toHaveBeenCalledTimes(1)
  })
})
```

- [ ] **Step 2: 运行,确认失败** Run: `npx vitest run packages/tools/src/lsp/manager.test.ts` — Expected: FAIL。

- [ ] **Step 3: 实现 manager.ts**

```ts
import { tmpdir } from 'node:os'
import path from 'node:path'
import { LspClient } from './client.js'
import type { LanguageServerConfig } from './servers.js'

/** 启动一个 client 的函数签名(默认 LspClient.start;测试可注入桩)。 */
export type ClientStarter = (
  config: LanguageServerConfig,
  cwd: string,
  dataDir: string | undefined,
  signal: AbortSignal,
) => Promise<LspClient>

/** 会话级语言服务器进程池:按语言懒启动 + 复用 + 退出清理。 */
export class LspManager {
  private cwd: string | undefined
  private clients = new Map<string, LspClient>()
  private starting = new Map<string, Promise<LspClient>>()
  private cleanupArmed = false

  constructor(private starter: ClientStarter = LspClient.start) {}

  /** 首次 run 时由工具传入 ctx.cwd;之后忽略。 */
  setCwd(cwd: string): void {
    if (this.cwd === undefined) this.cwd = cwd
  }

  /** 取(或懒启动)某语言的 client。并发同语言只 spawn 一个。 */
  async getClient(config: LanguageServerConfig, signal: AbortSignal): Promise<LspClient> {
    this.armCleanup()
    const cwd = this.cwd ?? process.cwd()
    const existing = this.clients.get(config.id)
    if (existing) return existing
    const inflight = this.starting.get(config.id)
    if (inflight) return inflight
    const dataDir = config.dataDirArg ? path.join(tmpdir(), `zuse-lsp-${config.id}`) : undefined
    const p = this.starter(config, cwd, dataDir, signal)
      .then((c) => {
        this.clients.set(config.id, c)
        this.starting.delete(config.id)
        return c
      })
      .catch((e) => {
        this.starting.delete(config.id)
        throw e
      })
    this.starting.set(config.id, p)
    return p
  }

  /** 进程退出兜底:杀掉所有存活 client(来不及优雅 shutdown)。注册一次。 */
  private armCleanup(): void {
    if (this.cleanupArmed) return
    this.cleanupArmed = true
    const kill = (): void => { for (const c of this.clients.values()) void c.dispose() }
    process.once('exit', kill)
    process.once('SIGINT', kill)
  }

  /** 优雅关闭所有 client(会话结束时调)。 */
  async dispose(): Promise<void> {
    const all = [...this.clients.values()]
    this.clients.clear()
    await Promise.all(all.map((c) => c.dispose()))
  }
}
```

- [ ] **Step 4: 运行,确认通过** Run: `npx vitest run packages/tools/src/lsp/manager.test.ts` — Expected: PASS。
- [ ] **Step 5: typecheck + lint** Run: `pnpm -r typecheck && npx eslint packages/tools/src/lsp/manager.ts`
- [ ] **Step 6: 暂存** `git add packages/tools/src/lsp/manager.ts packages/tools/src/lsp/manager.test.ts`

---

## Task 8: createLspTool —— 单个 Lsp 工具(index.ts)

**Files:**
- Create: `packages/tools/src/lsp/index.ts`
- Test: `packages/tools/src/lsp/tool.test.ts`(错误路径用假 manager 测;成功路径留 Task 9)

- [ ] **Step 1: 写失败测试(错误路径,纯逻辑)**

```ts
import { describe, it, expect } from 'vitest'
import { createFileTracker, type ToolContext } from '@zuse/core'
import { createLspTool } from './index.js'
import { LspManager } from './manager.js'

function makeCtx(cwd = process.cwd()): ToolContext {
  return { cwd, signal: new AbortController().signal, tracker: createFileTracker() }
}

describe('Lsp tool — error paths', () => {
  const tool = createLspTool(new LspManager(async () => { throw new Error('should not start') }))

  it('is readOnly', () => {
    expect(tool.readOnly).toBe(true)
    expect(tool.name).toBe('Lsp')
  })
  it('errors on unsupported file type', async () => {
    const r = await tool.run({ operation: 'definition', file: 'a.docx', symbol: 'x' }, makeCtx())
    expect(r.isError).toBe(true)
    expect(r.output).toMatch(/unsupported|not support/i)
  })
  it('errors when the file cannot be read', async () => {
    const r = await tool.run({ operation: 'hover', file: 'no-such-file.ts', symbol: 'x' }, makeCtx())
    expect(r.isError).toBe(true)
  })
  it('errors on missing required fields', async () => {
    const r = await tool.run({ operation: 'definition' }, makeCtx())
    expect(r.isError).toBe(true)
  })
})
```

- [ ] **Step 2: 运行,确认失败** Run: `npx vitest run packages/tools/src/lsp/tool.test.ts` — Expected: FAIL。

- [ ] **Step 3: 实现 index.ts**

```ts
import { readFileSync } from 'node:fs'
import { resolvePath, type Tool, type ToolContext, type ToolResult, type JSONSchema } from '@zuse/core'
import { lookupLanguage } from './servers.js'
import { locateSymbol } from './symbol.js'
import { formatDefinition, formatReferences, formatHover, type LineReader } from './format.js'
import { LspError } from './client.js'
import type { LspManager } from './manager.js'

const REFERENCES_LIMIT = 100

interface LspInput {
  operation?: 'definition' | 'references' | 'hover'
  file?: string
  symbol?: string
  line?: number
}

const inputSchema: JSONSchema = {
  type: 'object',
  properties: {
    operation: {
      type: 'string',
      enum: ['definition', 'references', 'hover'],
      description:
        "Which query: 'definition' jumps to where the symbol is defined; 'references' lists all uses; 'hover' shows its type/signature/doc.",
    },
    file: { type: 'string', description: 'Path to the source file containing the symbol.' },
    symbol: { type: 'string', description: 'The identifier to look up (e.g. a function or variable name).' },
    line: {
      type: 'number',
      description: 'Optional 1-based line number to disambiguate when the symbol appears multiple times.',
    },
  },
  required: ['operation', 'file', 'symbol'],
}

/** 按绝对路径建一个「读第 line0 行(0-based)源码」的 LineReader,带小缓存避免重复读盘。 */
function makeLineReader(): LineReader {
  const cache = new Map<string, string[]>()
  return (absPath: string, line0: number): string => {
    let lines = cache.get(absPath)
    if (!lines) {
      try { lines = readFileSync(absPath, 'utf8').split('\n') } catch { lines = [] }
      cache.set(absPath, lines)
    }
    return lines[line0] ?? ''
  }
}

/**
 * Lsp 工具:对项目代码做只读的定义跳转 / 找引用 / 悬停类型查询。
 * 需要会话级进程池 LspManager,用工厂闭包注入(对齐 createWebSearchTool)。
 */
export function createLspTool(manager: LspManager): Tool {
  return {
    name: 'Lsp',
    description:
      'Read-only code intelligence over the project via a language server. ' +
      'Look up a symbol\'s definition, find all references, or get its type/hover info. ' +
      'Give the file, the symbol name, and optionally a 1-based line to disambiguate.',
    inputSchema,
    readOnly: true,
    specifierFor: (input: unknown): string | null => {
      const f = (input as LspInput).file
      return typeof f === 'string' ? f : null
    },

    async run(rawInput: unknown, ctx: ToolContext): Promise<ToolResult> {
      const input = (rawInput ?? {}) as LspInput
      if (!input.operation || !input.file || !input.symbol) {
        return { output: 'Lsp requires operation, file, and symbol.', isError: true }
      }
      if (ctx.signal.aborted) return { output: 'Lsp cancelled.', isError: true }

      const config = lookupLanguage(input.file)
      if (!config) {
        const exts = '.ts .tsx .js .jsx .py .vue .java .rs .go .lua .sh …'
        return { output: `Lsp: unsupported file type for ${input.file}. Supported: ${exts}`, isError: true }
      }

      const abs = resolvePath(ctx.cwd, input.file)
      let text: string
      try {
        text = readFileSync(abs, 'utf8')
      } catch (e) {
        return { output: `Lsp: cannot read ${input.file}: ${(e as Error).message}`, isError: true }
      }

      const loc = locateSymbol(text, input.symbol, input.line)
      if (!loc) {
        return {
          output: input.line
            ? `Lsp: symbol '${input.symbol}' not found on line ${input.line} of ${input.file}.`
            : `Lsp: symbol '${input.symbol}' not found in ${input.file}.`,
          isError: true,
        }
      }

      manager.setCwd(ctx.cwd)
      const readLine = makeLineReader()
      try {
        const client = await manager.getClient(config, ctx.signal)
        client.openDocument(abs, text)
        if (input.operation === 'definition') {
          const locs = await client.definition(abs, loc.position)
          return { output: formatDefinition(locs, ctx.cwd, readLine, input.symbol), isError: false }
        }
        if (input.operation === 'references') {
          const locs = await client.references(abs, loc.position)
          return { output: formatReferences(locs, ctx.cwd, readLine, REFERENCES_LIMIT, input.symbol), isError: false }
        }
        const hover = await client.hover(abs, loc.position)
        return { output: formatHover(hover, input.symbol), isError: false }
      } catch (e) {
        if (ctx.signal.aborted) return { output: 'Lsp cancelled.', isError: true }
        if (e instanceof LspError) {
          return { output: e.installHint ? `${e.message}` : `Lsp error: ${e.message}`, isError: true }
        }
        return { output: `Lsp error: ${(e as Error).message}`, isError: true }
      }
    },
  }
}
```

- [ ] **Step 4: 运行,确认通过** Run: `npx vitest run packages/tools/src/lsp/tool.test.ts` — Expected: PASS。
- [ ] **Step 5: typecheck + lint** Run: `pnpm -r typecheck && npx eslint packages/tools/src/lsp/index.ts`
- [ ] **Step 6: 暂存** `git add packages/tools/src/lsp/index.ts packages/tools/src/lsp/tool.test.ts`

---

## Task 9: TS 集成测试(真起 typescript-language-server)

**Files:**
- Create: `packages/tools/src/lsp/integration.test.ts`
- Create: `packages/tools/src/lsp/__fixtures__/sample.ts`(测试夹具)

- [ ] **Step 1: 写夹具**

`packages/tools/src/lsp/__fixtures__/sample.ts`:
```ts
export function greet(name: string): string {
  return `hello ${name}`
}

const who = 'world'
export const message = greet(who)
```

- [ ] **Step 2: 写集成测试**

`packages/tools/src/lsp/integration.test.ts`:
```ts
import { describe, it, expect, afterAll } from 'vitest'
import path from 'node:path'
import { fileURLToPath } from 'node:url'
import { createFileTracker, type ToolContext } from '@zuse/core'
import { findOnPath } from '../util.js'
import { createLspTool } from './index.js'
import { LspManager } from './manager.js'

const here = path.dirname(fileURLToPath(import.meta.url))
const fixtureDir = path.join(here, '__fixtures__')
// typescript-language-server 是 devDependency,装在 node_modules/.bin。
const hasServer = !!findOnPath(process.platform === 'win32' ? 'typescript-language-server.cmd' : 'typescript-language-server')

function makeCtx(): ToolContext {
  return { cwd: fixtureDir, signal: new AbortController().signal, tracker: createFileTracker() }
}

const manager = new LspManager()
const tool = createLspTool(manager)
afterAll(async () => { await manager.dispose() })

describe.skipIf(!hasServer)('Lsp tool — TS integration', () => {
  it('finds the definition of greet from its call site', async () => {
    const r = await tool.run({ operation: 'definition', file: 'sample.ts', symbol: 'greet', line: 6 }, makeCtx())
    expect(r.isError).toBeFalsy()
    expect(r.output).toMatch(/sample\.ts:1:/)
  }, 60_000)

  it('finds references to greet', async () => {
    const r = await tool.run({ operation: 'references', file: 'sample.ts', symbol: 'greet' }, makeCtx())
    expect(r.isError).toBeFalsy()
    expect(r.output).toMatch(/reference/i)
  }, 60_000)

  it('returns hover type info for greet', async () => {
    const r = await tool.run({ operation: 'hover', file: 'sample.ts', symbol: 'greet', line: 1 }, makeCtx())
    expect(r.isError).toBeFalsy()
    expect(r.output).toMatch(/greet|string/)
  }, 60_000)
})
```

> `findOnPath` 只查系统 PATH,未必含 `node_modules/.bin`。若 `hasServer` 为 false 导致全跳过:在测试里改为显式解析 `node_modules/.bin/typescript-language-server(.cmd)` 的存在性(用 `existsSync` + 仓库根定位),并把该绝对路径通过临时改 `PATH` 或自定义 starter 注入。实现者跑通为准 —— 目标是这三条真的执行并通过,而非永远 skip。

- [ ] **Step 3: 运行集成测试** Run: `npx vitest run packages/tools/src/lsp/integration.test.ts`
Expected: 3 条 PASS(若环境无 server 则需按上述注解修正解析逻辑使其能找到 devDependency)。

- [ ] **Step 4: 暂存** `git add packages/tools/src/lsp/integration.test.ts packages/tools/src/lsp/__fixtures__/sample.ts`

---

## Task 10: 接线到 registry 与 TUI

**Files:**
- Modify: `packages/tools/src/index.ts`(导出 + 注册)
- Modify: `packages/tui/src/App.tsx`(构建 LspManager 并传入)
- Test: `packages/tools/src/index.test.ts`(断言 Lsp 已注册)

- [ ] **Step 1: 写/改 registry 测试**

向 `packages/tools/src/index.test.ts` 追加:
```ts
import { LspManager } from './lsp/manager.js'
// 在现有 describe 内:
it('registers the Lsp tool when an LspManager is provided', () => {
  const registry = createDefaultRegistry({ lsp: new LspManager() })
  expect(registry.get('Lsp')).toBeTruthy()
})
it('omits Lsp when no manager is provided', () => {
  const registry = createDefaultRegistry()
  expect(registry.get('Lsp')).toBeUndefined()
})
```
(确认文件顶部已 import `createDefaultRegistry`。)

- [ ] **Step 2: 运行,确认失败** Run: `npx vitest run packages/tools/src/index.test.ts` — Expected: FAIL(`lsp` 选项不存在 / 未注册)。

- [ ] **Step 3: 改 index.ts**

在 `packages/tools/src/index.ts`:
```ts
// 顶部新增 import
import { createLspTool } from './lsp/index.js'
import { LspManager } from './lsp/manager.js'
// 导出
export { createLspTool } from './lsp/index.js'
export { LspManager } from './lsp/manager.js'
```
在 `DefaultRegistryOptions` 接口加字段:
```ts
  /** LSP 进程池;传入时注册 Lsp 工具(无条件可注册,没装服务器是运行时错误)。 */
  lsp?: LspManager
```
在 `createDefaultRegistry` body 末尾(return 前)加:
```ts
  if (opts.lsp) registry.register(createLspTool(opts.lsp))
```

- [ ] **Step 4: 运行,确认通过** Run: `npx vitest run packages/tools/src/index.test.ts` — Expected: PASS。

- [ ] **Step 5: 接线 TUI**

读 `packages/tui/src/App.tsx`,找到第 36 行附近的 `createDefaultRegistry({ webSearch: ... })`。改为:
```tsx
import { createDefaultRegistry, LspManager } from '@zuse/tools'
// ...在组件内,与 registry 同一个 useMemo 作用域:
const registry = useMemo(() => {
  const lsp = new LspManager()
  return createDefaultRegistry({ webSearch: getWebSearchConfig(resolved), lsp })
}, [resolved])
```
> 若需在会话结束时优雅关闭,可在 useEffect 的 cleanup 里持有 manager 引用并 `void manager.dispose()`;但 manager 已自带 process exit/SIGINT 兜底,TUI 不接 cleanup 也不会泄漏孤儿进程。保持改动最小:仅替换这一处 useMemo。

- [ ] **Step 6: 全量检查**

Run: `pnpm -r typecheck && npm test && npx eslint packages/tools/src packages/tui/src`
Expected: typecheck 无错;全部既有 208 + 新增用例通过;lint 干净。

- [ ] **Step 7: 暂存** `git add packages/tools/src/index.ts packages/tools/src/index.test.ts packages/tui/src/App.tsx`

---

## 收尾(不提交)

- [ ] 跑一次完整 `npm test` 与 `pnpm -r typecheck`,确认全绿。
- [ ] `git status` 列出所有改动,**保持 staged/未提交**,等用户评审。
- [ ] 向用户汇报:实现完成、检查结果、未提交、以及实测时发现的任何偏差(尤其 Task 1 的导入形式、Task 6 的 `$/progress` / shutdown 入参实测结论、Task 9 集成测是否真跑通)。
